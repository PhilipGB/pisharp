# PiSharp capability parity — durable execution state

**Goal:** provide Pi's capabilities in an idiomatic C#/.NET implementation, with behavioral and differential evidence; do not claim parity while material gaps remain. Pi Packages are the only consciously excluded ecosystem feature unless audit shows they affect core capability.

**Pinned upstream:** `earendil-works/pi@d5629e20489ccf770ed90b5a33941cb3b7ef24d0` (fetched `main` 2026-09-24). Previous pin: `a7d17e39aaa0091c7573d0790714751956f10bd1`; earlier `7c696c00f34cf773c86d33093de8d5711994c5e4`. The delta adds TUI color values and theme styling helpers, OSC 10/11 foreground/background replies, wrapper-aware `@` path autocomplete and durable execution changes. Track these in TUI/theme work; Jev classifier/OpenRouter/Cloudflare/System One from the previous pin remains open. Refresh again before final parity claim.

## Current state and audit evidence

At reprioritization, clean `main` was `fa162970f`; after the OSC checkpoint, `e8a557d80` passed local format/warnings-as-errors build, 255 tests, and Linux CI. Later checkpoints require their own validation. These are local checks, not complete differential evidence. Tracking is partial: targeted stale multimodal, command, key and RPC rows have now been reclassified with local evidence, not upstream differential verification. Further empty ledger rows still require evidence review. There is no evidence for full feature parity. In particular the README explicitly calls out absent OAuth, wide provider semantics, Pi JSONL, full TUI, broad settings/keybindings, comprehensive multimodal support, rich resources/extensions and Pi-compatible RPC.

## Highest-priority actionable work (user reprioritized: finish core, tools, TUI first)

Use **OpenAI Responses and llama.cpp's OpenAI-compatible Chat Completions** as the working backends. Maintain and test their existing request paths, but defer new hosted-provider adapters, OAuth, extended catalog coverage and other provider expansion until core/tools/TUI are complete. Do not confuse this sequencing with a parity claim.

1. **Agent core and recovery (highest):** complete the agent turn/tool loop, retry/cancellation/steering/follow-up semantics, in-tool-loop context budgeting and compaction, overflow recovery, durable tool outcomes, token/usage accounting and branch/session consistency. Test actual OpenAI/llama-compatible request paths with deterministic loopback servers, fake-client lifecycle tests, and pinned Pi differential cases. Do not re-execute uncertain side effects or replace canonical raw history.
2. **Coding tools (next):** complete read/write/edit/bash/grep/find/ls schemas and edge cases (images, encodings/BOM/CRLF, symlinks/permissions/atomicity, glob/ignore behavior, truncation and continuation, live output, process-group cancellation and result metadata). Add bounded pinned upstream differentials and end-to-end agent tool-turn fixtures.
3. **TUI/editor/keybindings (next):** replace editor-only UX with a maintainable transcript/rendering architecture for streaming text, reasoning, markdown/code/tool blocks, status, overlays/pickers/settings, resize, scroll/search, mouse, clipboard/selection/external editor, normal/fullscreen restoration and images. Add PTY snapshots and interactive behavior tests, including OpenAI/llama-backed tool turns.
4. **Core-adjacent completion:** finish Pi JSONL session interoperability, crash consistency and fork/clone/branch behavior, RPC/JSON event and command compatibility, context/settings/keybinding interactions and CLI workflows needed to make agent/tools/TUI complete. Keep the feature matrix and detailed ledger scoped to evidence.
5. **Deferred provider/model/auth and multimodal breadth:** after the above, resume native hosted-provider adapters, OAuth/refresh, model APIs/catalogs, classifier and provider-wide multimodal transport; retain OpenAI and llama.cpp working throughout.
6. **Final audit:** refresh upstream SHA, audit full source/docs/tests/interfaces (agent, tools, TUI, CLI, commands, settings, keybindings, providers/auth, images, sessions, compaction, resources, extensions, RPC/API and recovery); run format, warnings-as-errors build, all tests, differential and representative end-to-end tests; update matrix/inventory/ledger and remove unresolved parity items only when evidence supports it.

## Working rules

After each significant change: add/update tests and parity evidence, run focused tests, update this file and parity docs, commit coherent changes and push if established; then immediately select the next unresolved item. Never claim behavioral verification from code presence alone. Preserve exact upstream SHA for fixtures. No voluntary pause while actionable work remains.

## Immediate discovered tasks

- New priority checkpoint (`fa162970f`): stop expanding hosted providers; first close agent-core tool-loop budgeting/recovery, then tool contracts, then TUI transcript/rendering/interaction. Keep OpenAI Responses and llama.cpp-compatible Completions as regression backends. Do not promote existing ledger `verified` rows to upstream parity on local evidence. The first focused core change distinguishes a content-free streamed provider envelope from actual model output when deciding whether a pre-content retry is safe.
- Finish compaction: opt-in tool-loop continuation now projects a whole-turn summary only for the provider request, preserves canonical/raw tool history, checkpoints summary usage/projection, and refuses oversized unsummarizable continuations. A subsequent checkpoint adds one bounded pre-content retry for OpenAI/llama-compatible 400/413 context errors (fake-client cases plus deterministic OpenAI SDK Responses/Completions loopback HTTP 400 fixtures, no live provider); it never retries after content or for rate limiting. Still need hosted live-provider overflow and further llama.cpp-server cases (post-content transport failures and adversarial tool loops), adversarial multi-cycle long-turn splitting and bounded summary fidelity, cache invalidation across arbitrary changed prefixes outside steering and bounded-memory large histories (>1MB single messages or >16MB total), token-accurate limits, and pinned upstream differential evidence; pre-prompt persistent compaction remains separate.
- Live llama.cpp checkpoint: the user-provided server `http://192.168.0.97:8000/v1` exposes loaded `Qwen3.8-27B-GGUF` with `n_ctx=179200` via `/props?model=...`. A direct 190052-token Chat Completions request returned HTTP 400 `exceed_context_size_error` with `exceeds the available context size`; the opt-in `LiveLlamaOverflowTests` drove the real OpenAI .NET adapter through one failed streamed request, one nonstream summary, and one successful streamed retry, preserving the 950k-character raw old user turn. Run with `PISHARP_TEST_LLAMA_ENDPOINT=http://192.168.0.97:8000/v1 PISHARP_TEST_LLAMA_MODEL=Qwen3.8-27B-GGUF dotnet test tests/PiSharp.Tests/PiSharp.Tests.csproj --filter FullyQualifiedName~ActualLlamaServerContextOverflowRetriesWithSummaryAndPreservesRawHistory`. Default CI without the endpoint runs no live assertions (xUnit VSTest does not support dynamic skips). No hosted live-provider credential is available, and this one model/error path is not general backend parity.
- Live tool-turn checkpoint: `LiveLlamaOverflowTests.ActualLlamaServerReadsFileAndContinuesAfterToolResult` ran with the same opt-in endpoint/model; a real streamed model tool call used the built-in read tool and a follow-on streamed request completed with the sentinel in canonical tool history. This is one model-guided read turn; the separate following fixture tests live in-tool-loop overflow, not full tool contract parity.
- Live in-tool-loop overflow checkpoint: an opt-in real llama.cpp test uses a deterministic extension tool emitting 950k characters (190k `ping ` repetitions) in one turn. The streamed continuation receives the server's pre-content context 400, summarizes the completed tool cycle, retries once, and completes with exactly one tool invocation and the full result in canonical history. This is one benign model-directed tool call, not a guarantee for arbitrary parallel tools or post-content replay; cancellation has a separate subsequent fixture.
- Live cancellation checkpoint: an opt-in streamed request against the same llama.cpp server was cancelled on its first model text delta. Exactly one model request started, the turn was interrupted, and no retry/overflow-recovery/completed event was emitted. This tests user cancellation after content; a server-side post-content transport failure still needs evidence.
- Pinned overflow classifier differential slice: executed `isContextOverflow` from `/tmp/pisharp-upstream/packages/ai/src/utils/overflow.ts` at the pinned SHA using `node --import tsx`; Ollama prompt-too-long, parenthesized OpenAI-compatible max-context, llama.cpp's actual context error, rate limit and model-runner-crash returned `true,true,true,false,false`. Local fake HTTP 400 fixtures now match the first two plus the preexisting llama/rate cases and reject generic crashes. PiSharp intentionally gates recovery on HTTP 400/413, pre-content and a single retry; pinned Pi supports additional providers/status/silent overflow, so this is a scoped differential, not full parity.
- Parallel tool-batch core checkpoint: a local MAF client streams two read calls together; after both 4000-character results, one in-flight summary cuts at the completed batch, the next request has no orphan call/result, and canonical history retains both calls and both results. This adds one bounded parallel graph, not arbitrary graph, real summarizer fidelity or upstream equivalence.
- Post-content SDK transport checkpoint: a deterministic local SSE server sends one valid `partial` text delta, then malformed JSON and closes, for both OpenAI .NET Responses and Chat Completions clients. PiSharp emits one request, a failed turn, and an interrupted checkpoint retaining `partial`; neither transient retry nor context-overflow recovery fires. This is a real adapter/loopback post-content parse failure, not a live llama.cpp transport failure or pinned Pi differential.
- Core recovery checkpoint: a `run_finished(completed=false)` with checkpointed tool outcomes now produces an idempotent no-replay recovery notice on restart or before the next same-process prompt; successful runs remain unchanged. Focused fake-client/durable tests and full 259-test local suite, warnings-as-errors build, and format verification pass. No upstream differential or provider overflow replay evidence yet.
- Overflow checkpoint: fake-client OpenAI and llama.cpp error strings under HTTP 400 trigger at most one pre-content projection/retry; rate-limit-looking errors and partial output do not replay. Format and warnings-as-errors build pass; full suite passed 264/264 on rerun after one transient `NonStreamingResponsesDoNotEnableProviderOwnedHistory` failure in parallel (focused rerun passed). Investigate that test's parallel flake; the later SDK loopback fixture below closes the synthetic HTTP gap, but pinned Pi differential remains open.
- Investigate sporadic parallel subprocess/loopback failure in `ProviderModelRuntimeTests.ConfiguredProviderStreamsThroughCliWithoutCatalogOrCloudCredential` (base configuration). One full-suite run failed without diagnostic detail; all six focused variants and a verbose full-suite rerun passed. The process fixture now includes exit code and captured stdout/stderr on early CLI failure before waiting for the loopback; await a reproduction to classify it before changing production behavior.
- SDK overflow loopback checkpoint: `ProviderOverflowLoopbackTests` drives real OpenAI .NET SDK Responses and Chat Completions adapters through a local HTTP 400 error, a nonstreaming summary request, and a successful streamed retry. It asserts the third request contains the summary but not old raw user text, while canonical session history retains the old text. Format verification, warnings-as-errors build and all 266 local tests pass. This historical SDK-only checkpoint predates the separate live llama.cpp fixture; pinned upstream differential remains open.
- In-loop summary cache checkpoint: the same earlier-turn prefix in a single run reuses a successfully budgeted summary instead of repeating a billable summarization at each tool continuation. The original key held up to 64KB of serialized content; an MAF two-read fixture checks three model requests, one summary and intact tool results. Later steering and bounded-hash fixtures supersede the original key cap; other mutations remain open.
- Split-turn checkpoint: if the active user turn alone exceeds the configured estimate, in-flight projection can summarize its prefix only after a fully matched tool call/result cycle, keeping the raw tool result in canonical history. A single-turn 4000-character read fixture verifies the bounded continuation; failed summarization still fails closed, checkpoints the tool outcome, and issues a recovery notice on the next prompt. A later two-cycle fixture exercises repeated cuts; adversarial summaries and long-running fidelity remain open.
- Summary-cache invalidation fixture: steering queued synchronously during the first summary adds a new user boundary before the third provider request. Two reads then require two distinct summaries and preserve the steering prompt in canonical history. An initial test queued steering from the asynchronous tool-finished event consumer and raced the provider request; the fixture now queues while the summary request is in flight. No general guarantee of UI event-consumer synchronization is claimed.
- Bounded fingerprint checkpoint: SHA-256 of length-framed serialized messages replaces the raw cache key. A 70k-character older user turn now reuses one summary; a >1MB single message bypasses caching and re-summarizes. Keys retain only a digest; a per-message 1MB and aggregate 16MB cap bound fingerprinted bytes (serialization still materializes a message). These are local fixtures, not token-accurate or pinned-upstream parity.
- Repeated split-turn fixture: a 4000-character first read and a second small read within the same active turn trigger two bounded summaries; three model requests complete, and both raw tool results remain in canonical history. Summary content is fake and does not prove fidelity or pinned Pi's cut-point selection.
- Core checkpoint: `ObservedChatClient` no longer treats content-free SSE metadata envelopes as model output for pre-content retries; a fake-client lifecycle test proves a retry and exactly one persisted assistant message. Local format/warnings-as-errors build/full 252-test suite passes. At that checkpoint in-loop compaction was still absent; the later transient projection above is deliberately limited and does not replace durable history.
- Agent-core sequence: verify streamed retry/partial response boundary; implement safe in-tool-loop budget and compaction without overwriting canonical history; test overflow and crash recovery across tool calls. Then tool read (including image/encoding/continuation), write/edit atomicity and bash streaming/cancellation, followed by TUI transcript architecture and PTY snapshots. Provider retry adapter expansion listed below is deferred.
- Continue targeted ledger reconciliation beyond the corrected multimodal/dynamic/interactive/RPC/key subsets (the ledger's `verified` agent row denotes local evidence, not upstream differential verification). Preserve honest states.
- Existing `defaultProjectTrust` and four compaction settings ledger entries now carry scoped implementation/evidence notes rather than empty arrays; in-loop compaction and upstream differential requirements remain open.
- Five implemented settings ledger entries (`defaultProvider`, `defaultModel`, `defaultThinkingLevel`, `defaultTools`, `sessionDir`) reclassified in-progress with UserSettings tests and trust-boundary process evidence; schema/reload/upstream differential acceptance remains open.
- Ledger `cli-sessions` reclassified from not-started to in-progress with concrete CLI/PTY references; `cli-resources` now records explicit-path and trust evidence. Continue remaining empty ledger entries rather than inferring verification.
- Stale ledger CLI tools/resources/runtime groups reclassified in-progress based on concrete CLI parser/runtime and process tests; remaining flags in those groups are explicitly absent. Continue auditing every ledger item instead of treating groups as verified.

- Reconciliation checkpoint: reclassified multimodal, dynamic/interactive command, implemented RPC and partial keyboard rows with concrete code/tests and explicit remaining gaps; no row was promoted to Verified. README's extension-path contradiction corrected. More empty ledger entries still need scoped reconciliation, not a new large audit.
- CI investigation: `.github/workflows/ci.yml` still has push/PR triggers, but repository Actions permissions API returned `enabled: false`; re-enabled Actions via repository API on 2026-09-24. The next push (`5435cbeeb`) received successful Linux CI run `36013464320` (format, warnings-as-errors build, full tests). No workflow-file fix was required.
- Provider architecture in progress: `ProviderChatClientFactory` selects OpenAI Responses instead of assuming Chat Completions for every provider, while retaining compatible adapters for OpenRouter/Mistral/custom and a configured Responses opt-in. `StatelessResponsesChatClient` discards provider-owned conversation IDs to preserve MAF canonical history. Loopback SSE, Responses function-call/tool-result continuation and protocol-selection fixtures pass; no broad hosted provider/auth parity, Responses usage/reasoning/image differential or OAuth yet.
- Current checkpoint validation: `dotnet format PiSharp.slnx --verify-no-changes --no-restore`, `dotnet build PiSharp.slnx --warnaserror --no-restore` and `dotnet test PiSharp.slnx --no-build --no-restore` pass 234/234 including static xAI catalog and loopback Responses PNG data-URL transport. The push of `b3e0ccc93` initially returned transient GitHub HTTP 500 but subsequently succeeded; no repository change was required. These are local tests, not full provider or pinned Pi differential evidence.
- Extracted bounded `models.json` provider parsing/validation into `ProviderProfileLoader` to keep `ProviderModelRuntime` focused on selection and catalog/auth. Added xAI API-key Responses profile with pin-derived Grok 4.3/4.5/4.6/4.7 price tiers/modality metadata and model-ID override (no invented metadata for unknown IDs); xAI catalog listing uses pinned static entries instead of assuming `/models` support. Deterministic credential-isolation and static-catalog tests pass. Extracted profile definitions into `BuiltinProviderProfiles`. Disallow built-in OpenRouter/Mistral/xAI endpoint redirects and cross-provider use of their key environment names. xAI subscription OAuth and live provider semantics remain absent.
- Provider continuation after `d70baac13`: native Anthropic Messages adapter via Anthropic .NET SDK 12.45.0 and a built-in API-key-only `anthropic` profile (`claude-sonnet-4-6` unpriced default, `PISHARP_ANTHROPIC_MODEL` override). Uses a static configured catalog, not the incompatible OpenAI `/models` parser; refuses reserved built-in endpoint/key-env redirects. Local loopback nonstream and SSE tests assert `/v1/messages`, `x-api-key` instead of Bearer, version header, body, usage and streamed text. No live account, tool/image/reasoning semantics, retry/header policy, model-specific metadata beyond configured IDs, OAuth or pinned Pi differential. `dotnet format --verify-no-changes --no-restore`, `dotnet build --warnaserror --no-restore` and the full local suite passed 238/238 including the Anthropic built-in endpoint-isolation regression. Continue provider work after committing.
- Anthropic follow-up (after `c5d4c0778`, Linux CI passed): loopback fixture covers SSE tool use/MAF echo tool result continuation, and another verifies PNG DataContent becomes a native base64 `image` block; metadata for pinned Sonnet 4.6, Opus 4.6 and Haiku 4.5 now includes context/output/modalities/base cache prices. Unknown override IDs remain unpriced. Full format verification, warnings-as-errors build and 240/240 local tests passed; committed as `1fd564c68` and Linux CI passed. Native Anthropic adapter now passes model-specific max output to Messages (loopback assertion), matching pinned default request-budget intent. Live-account, provider-specific reasoning/cache/retries and pinned Pi differential remain open.
- Anthropic reasoning follow-up: SDK 12.45.0 used `thinking.type=enabled` for all high `ReasoningOptions`, but pinned Pi requires adaptive for Sonnet/Opus 4.6 and extended for Haiku 4.5. Upgraded Anthropic to 12.50.0 and selected the SDK's `AnthropicThinkingMode` per pinned model; loopback theory verifies adaptive + high `output_config.effort` for Sonnet/Opus and enabled + token budget for Haiku. Unknown/custom Anthropic IDs still default to extended without capability metadata; broader model mapping, effort-level differential, streaming thought, cache usage and live calls remain open. Format verification, warnings-as-errors build and 243/243 local tests passed; committed as `d618683d7` and Linux CI passed.
- Provider identity follow-up after `d618683d7` (Linux CI passed): case-variant built-in `models.json` override now preserves canonical provider ID for transport dispatch, auth storage, and model owner; a focused test confirms `ANTHROPIC` with a new model still selects Anthropic Messages and its isolated API key. Previously this could silently fall back to Chat Completions. Format verification, warnings-as-errors build and all 244 local tests passed; committed as `f090f493a`, Linux CI passed.
- Provider retry follow-up after `f090f493a` (Linux CI passed): `ProviderRetryPolicy` now classifies HTTP 408/409/429/5xx (and no-response transport) as transient, while 400/401/403 fail without replay; recognizes `System.ClientModel.ClientResultException` status for OpenAI SDK and `HttpRequestException.StatusCode`. Seven fake-client lifecycle cases plus format/warnings-as-errors build/full 251-test suite pass. Anthropic SDK exposes `AnthropicApiException.StatusCode` through a separate type: its built-in SDK retry behavior and PiSharp's outer retry are not yet coordinated; no Retry-After/header/backoff/abort parity and no direct OpenAI SDK status fixture. Committed as `178150266` and Linux CI passed; add Anthropic status handling with provider-specific boundary and fixtures before claiming provider retry parity.
- Updated TUI/theme TODO: cover color-value schema/styling helpers, OSC 10/11 foreground/background parsing, terminal true-color behaviour, and `@` path completion after wrappers/backticks against refreshed pin; inspect durable delta separately for relevant crash/recovery changes.
- TUI OSC safety slice: TerminalInput discards OSC BEL/ST terminal replies instead of leaking color query responses into the editor; bounded input fixtures cover complete, truncated, and oversized sequences. Still need terminal color querying/parsing and theme styling against the pinned upstream, plus PTY and renderer coverage. The csharp-ls duplicate Fact/Theory diagnostic in TerminalInputTests was fixed; warnings-as-errors build passed.

## Completed in this continuation

- `--list-models` now ignores `enabledModels` and explicit `--models` selection scopes so the full configured catalog remains discoverable (offline CLI fixture). Model catalog completeness and pinned Pi process differential remain open.
- Bounded user/trusted-project `enabledModels` arrays now supply model scope unless `--models` overrides (empty project array clears it). Unit validation/overlay and offline CLI subprocess tests pass. PiSharp scope currently restricts model resolution; pinned Pi's cycling-focused selection/thinking suffix/order and full project-setting semantics still need reconciliation.
- `settings.json` `quietStartup` hides the PiSharp terminal banner and `--verbose` overrides it; boolean/overlay tests plus a Linux PTY fixture confirm both paths. Pinned Pi uses the same controls for more notices and a different TUI; no full startup/reload differential yet.
- `settings.json` now validates `hideThinkingBlock` (default visible), merges trusted-project override, and suppresses interactive stderr reasoning deltas while leaving history/events intact. Local parse/overlay/invalid tests only; pinned Pi renders a collapsible thinking block in its full TUI, and no PTY rendering/reload differential exists.
- Scoped model patterns now distinguish `*` from cross-segment `**`, match simple bracket ranges/negation and avoid hidden segments, with a pinned minimatch Node differential slice (`docs/parity/model-scope-globs.md`) plus C# integration tests. Brace/extglob/escape semantics, thinking suffixes, ordering and full CLI parity remain open.
- Redirected non-RPC stdin is now limited to 1M UTF-16 characters before prompt composition, with trimmed/boundary/cancellation tests and a CLI subprocess proving oversized input exits 2 before provider auth/inference. This is a PiSharp safety bound, not pinned Pi stdin parity; streamed RPC input is unaffected.
- CLI `--list-models` now sorts filtered rows by provider/ID and emits Pi's no-match message for an unmatched pattern; offline subprocess fixture checks no-match stdout and zero stderr. Table columns and catalog coverage still differ.
- Differential slice: executed pinned `packages/tui/src/fuzzy.ts` directly under Node 24 for seven provider/model search patterns; all seven match/empty cases agree with local C# `CliModelFilterTests` on the same fixture. Score order, Unicode edge cases, full CLI table and catalog remain unverified.
- `--list-models [pattern]` now consumes the optional next nonflag/non-@ token and filters provider plus ID using pinned `packages/tui/src/fuzzy.ts` token/subsequence and alpha-digit-swap rules (local cases and CLI subprocess with unavailable endpoint). Pi's model table and remote catalogue behavior still differ.
- Explicit skill/template resource paths now take precedence over discovered same-name resources, with collision fixtures. Pinned `resource-loader.ts` merges CLI sources before enabled discovery and `skills.ts` keeps first winner; broader package-resolution/diagnostics semantics remain open.
- Repeatable `-e`/`--extension <path>` selects .NET DLLs or directories independently of `--no-extensions`, including on reload; a local isolated-load fixture and Linux PTY fixture prove explicitly selected untrusted-project code executes only when requested. Paths are full-trust code, not Pi TypeScript extensions. Broad extension API parity and upstream path semantics remain open.
- Repeatable `--skill <path>` and `--prompt-template <path>` select explicit files/directories even with automatic discovery disabled; the same paths reload. Bounded parsing, fail-closed missing paths, local untrusted-project fixture and HTTP provider-process request evidence; upstream path resolution/diagnostics/precedence remain open.
- `--offline` and truthy `PI_OFFLINE` now suppress dynamic OpenAI-compatible `/models` HTTP reads for discovery/listing while using configured/built-in models; a local fake HTTP fixture shows no catalog request and fuzzy configured selection, and subprocess fixtures verify both CLI and environment forms with an unreachable endpoint. This does not block inference traffic, auth refresh (not implemented), or establish upstream offline parity.
- CLI trust aliases `-a`/`-na` and upstream `--mode text` now parse; text maps to existing TTY-sensitive interactive/print lifecycle, not a Pi protocol match. Parser conflict tests only; mode behavior and Pi CLI differential remain open.
- CLI `-h/--help` and `-v/--version` work without reading invalid settings (subprocess fixture), and `-p`/`-c` aliases parse; version reports PiSharp assembly version rather than Pi's version. Other CLI flags, resume picker, and mode equivalence remain open.
- CLI `--no-skills`/`-ns` and `--no-prompt-templates`/`-np` independently skip user/trusted-project discovery and remain disabled on reload. Local parser/catalog tests verify command/context removal and skipped oversized templates; an HTTP CLI process fixture compares provider request content with discovery enabled versus both disabled. Upstream explicit-path semantics and differential evidence remain open.
- CLI `--no-extensions`/`-ne` now skips user/project DLL discovery at startup and reload; loader and PTY tests show trusted project commands are disabled; other resource discovery remains wired independently. Upstream explicit `-e` path semantics and full extension parity remain open.
- CLI `--no-builtin-tools`/`-nbt` now disables default built-ins while retaining extension tools; explicit allowlist can re-enable a built-in, and exclusion still removes extension tools. Local parser and provider tool-loadout fixtures; full upstream selector behavior remains unverified.
- CLI now reads trimmed redirected stdin even when a positional prompt is present (except RPC), prepending it before @file content and prompt, matching pinned Pi `main.ts`/`cli/initial-message.ts` composition; local HTTP subprocess asserts actual provider message order. No broad input-mode differential or stdin size bound yet.
- Anonymous configured providers no longer borrow `PISHARP_API_KEY` implicitly from the local endpoint; only explicit `apiKeyEnv` or provider-specific keys can select a secret. Local auth/connection isolation tests also preserve explicit local-endpoint key use. Broader provider credential precedence and process evidence remain open.
- Bounded OpenAI-compatible `/models` parsing now deduplicates via an ordinal set rather than quadratic scans; 8k-entry fixture preserves first occurrence and all distinct IDs. Local scalability only; remote catalog protocol still absent.
- Model scope wildcard matching (`*`, `?`, case-insensitive) now uses greedy wildcard matching rather than exponential regex backtracking; local scope fixture covers provider prefixes and a many-star rejection. Upstream scope semantics remain unverified.
- Configured provider `models.json` now has a 1MB read cap and rejects case-insensitive duplicate provider/model identities, invalid positive-integer metadata and OAuth declarations without an actual refresh adapter; local invalid-config fixtures. Continue auditing config schema/locks and provider auth parity.
- Stored OAuth credentials are now rejected for API-key-only providers instead of passed as raw bearer tokens or silently replaced by ambient API keys. Local credential-boundary test; genuine provider OAuth adapters and refresh remain outstanding.
- OpenAI-compatible model catalog parsing ignores malformed optional numeric metadata without aborting valid sibling entries; deterministic fixture. This is resilience evidence only, not provider parity.
- CLI `--no-context-files`/`-nc` skips AGENTS/CLAUDE loading at startup and reload without disabling unrelated resources; process test verifies oversized project context is not read. Full resource CLI parity remains open.
- CLI `--system-prompt` and repeatable `--append-system-prompt` now resolve UTF-8 files or literals, override discovered sources, and refresh files on reload. Local CLI parsing/resource precedence tests and an HTTP CLI process fixture asserting actual provider system content pass; reload and upstream differential evidence remain open.
- User-only `defaultProjectTrust` now resolves after explicit/stored decisions and before trusted-project settings are read; invalid/project-scope values fail closed. Local trust precedence and config validation fixtures pass. Project trust remains permission gating, not a sandbox.
- Implemented explicit offline `auth print-api-key --provider` for stored/configured/environment API keys only, stdout-only on success; missing/OAuth credentials never printed. Process test proves explicit retrieval, no cross-provider key leakage, and failure on missing key. OAuth bearer printing/refresh, default-model selection and Pi auth protocol remain open.
- Exact `provider/model` compaction reserve/recent overrides now merge across user/trusted-project scopes with explicit env window/reserve precedence; malformed entries fail closed and model-switch paths resolve policies. Differential provider-token accuracy remains unverified.
- `compaction.keepRecentTokens` selects recent whole user turns (including tool call/results), with settings validation and preserved raw history; local budget/rollback tests pass. This is a conservative heuristic rather than upstream tokenizer-exact retention. In-loop compaction and differential token accounting remain open.
- Image setting `images.blockImages` has local provider-boundary and history-retention evidence; still verify tool-result image filtering and upstream differential; resumed session provider-boundary filtering is locally tested. `autoResize` and image tool/clipboard/terminal support remain open.
- Added validated user `compaction.enabled` and `compaction.reserveTokens` with explicit context environment override, known-model context budget, malformed/duplicate rejection and local tests. Trusted-project settings now overlay the validated subset after trust resolution; untrusted project JSON is never read. Pre-prompt only, no upstream differential proof; process trust fixture and local precedence fixtures added.
- Expanded the provider model descriptor with model name, API identifier, declared input modalities, and max output tokens; load these from local catalogues (including OpenRouter `architecture.input_modalities`) and configured `models.json`, and preserve configured fallback metadata when the live endpoint omits it. Unit fixtures added. This metadata is descriptive only: native provider request adapters and image transport remain open.
- Added Pi-style request-wide model pricing tiers to catalog/model cost metadata and usage accounting. The highest threshold strictly below input-token count applies rates to the full request; local catalog parsing and cost arithmetic tests cover below/above threshold behavior. Provider cache-write token counts from `UsageDetails.AdditionalCounts` and cache-write rates are accounted for at both base and tier prices; unrecognized additional usage keys remain ignored.
- Revalidated with format verification, warning-as-error build, and all 131 tests after provider metadata/pricing changes.
- Added OpenRouter and Mistral OpenAI-compatible built-in provider profiles with dedicated endpoints, credentials, default model overrides, and regression tests against cross-provider key leakage. These expand hosted endpoint coverage but are not native provider/API parity; format verification, warnings-as-errors build, and all 133 tests pass.
- OpenRouter `/models` pricing now also reads `input_cache_write` (per-token dollars, converted to per-million units), in addition to prompt/completion/cache-read prices. Catalog fixture covers this conversion; format, warning-as-errors build, and all 132 tests pass.
- Extended usage accounting to recognize cache-write token-count metadata, persist it in usage records/events, and apply base or request-wide tier cache-write rates. Added catalog and arithmetic assertions for base/tier cache-write rates. Format verification, warning-as-error build, and all 131 tests pass; PiSharp remains far from provider/auth parity.
- Added a validated user `settings.json` subset for default provider/model/thinking level and built-in `defaultTools`, including a 64KB bound, duplicate/unknown-key rejection, invalid/duplicate tool rejection, CLI and provider-environment precedence, and saved-session model preservation. No project settings, broad schema, or `/settings` UI yet; format verification, warnings-as-errors build, and all 143 tests pass.
- Extended CLI positional `@file` processing across print, JSON, and initial interactive prompts to preserve UTF-8 text plus supported PNG/JPEG/GIF/WebP images as MAF DataContent in persisted user messages. Image files are capped at 20MB, signature-checked, and refused when catalog metadata explicitly declares text-only input. Runtime fake-provider tests verify image delivery and session round-trip; CLI fixtures cover image parts and invalid signatures. Format verification, warning-as-error build, and all 135 tests pass. Resize/normalization, clipboard/editor paste, tool image reads, and provider-wide transport remain open.
- Validation after this change: `dotnet format PiSharp.slnx --verify-no-changes`, `dotnet build PiSharp.slnx --warnaserror`, and `dotnet test PiSharp.slnx` pass (130/130).
- Refreshed upstream baseline from the prior pin to `8676a0dcd8f9f6bca78835e63c8cd31493c4154d`; delta touches model/provider and image/classifier infrastructure, catalog protocol, Typesafe System One, provider composition and clipboard/X11 behavior.
- Reconciled pinned SHA in matrix, detailed inventory, and execution ledger. The matrix still has broader stale-status problems; a full evidence-based per-item ledger reconciliation remains open.
- Fixed the HTTP provider fixture's race-prone TCP-port reservation by retrying `HttpListener.Start()` on bind collisions. The initial parallel run exposed the collision; reruns now pass in parallel.
- Validation after fix: `dotnet format PiSharp.slnx --verify-no-changes`, `dotnet build PiSharp.slnx --warnaserror`, and `dotnet test PiSharp.slnx` all pass (129/129).

## Unresolved / parity cannot be claimed

All eleven work areas above remain open; PiSharp is expressly an early-development partial implementation. No final audit or full parity evidence exists.
