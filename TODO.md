# PiSharp capability parity — durable execution state

**Goal:** implement Pi's in-scope capabilities idiomatically in C#/.NET, with behavioral and differential evidence. Pi Packages are excluded unless needed for a core capability.

**Stop condition:** a full parity audit against current Pi `main` finds no material in-scope capability gaps, and required validation and differential workflows pass.

## Baseline

- Current PiSharp `main` includes selected-model `read` image resize profiles in commit `f882b25adf591b5fc433e0f5be31066e9c90c6c9`. Linux CI [run 36096847111](https://github.com/PhilipGB/pisharp/actions/runs/36096847111) passed for that commit on 2026-09-25, including format verification, the warnings-as-errors build and all 363 tests.
- Edit preview details were published in `beced084c`; Linux CI [run 36098970353](https://github.com/PhilipGB/pisharp/actions/runs/36098970353) passed. A locally validated write follow-up now canonicalizes existing mutation targets through directory symlinks; its Linux CI is pending the next push.
- Pinned Pi: `earendil-works/pi@d5629e20489ccf770ed90b5a33941cb3b7ef24d0`. Refreshed upstream `main` as `5fd446ca1843682e8da3fec4ceb71c42f56fbace`; inspected Bash and edit source/test paths are unchanged between pin and current main. Refresh before the final audit and relevant capability families.
- The selected model's optional image resize metadata now flows from configured/discovered catalogs to the `read` processor and provider boundary. Focused tests passed 51/51; the Release suite passed 363/363 with 0 skipped. Remaining `read` differences are listed in the matrix and final-audit notes.

## Priorities

1. **Coding tools (active):** complete `read`, `bash`, `edit`, `write`, `grep`, `find`, and `ls` contracts using upstream implementation/tests, deterministic local cases, pinned differentials, and real agent turns. Once major behavior is covered, move on rather than polishing obscure edge cases.
   - `read`: small and streamed text paths decode UTF-8 consistently, preserve BOMs, and avoid UTF-16 BOM auto-detection. Image processing decodes and validates, converts BMP to PNG, and uses 2000×2000 / 4.5 MiB base64 defaults. The selected model's optional `inputLimits.images.resize` profile now flows from configured or discovered metadata into the read tool, including its provider-boundary payload limit. Local Responses and Chat Completions tool turns cover pass-through and high-entropy images plus session reload. Still open: EXIF orientation, Lanczos3 equivalence, files over 20 MiB, animated-image transformations, exact metadata/errors, broader filesystem/cancellation cases, and pinned image request/result differentials.
   - `bash`: model-tool live text updates coalesce at 100ms; direct RPC Bash forwards each decoded output chunk immediately. Shell selection honors validated `shellPath`; combined stdout/stderr, inherited process environment, UTF-8 final flush, bounded tail/full spill, cwd and finite timeout validation, 100ms rearmed post-exit drain, and process-group timeout/abort cleanup have local unit, process, agent and JSON event evidence. Model-invoked Bash receives current `PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, and `PI_REASONING_LEVEL` through MAF per-run context, and Bash strips inherited stale values before starting a shell. Direct/session Bash strips Pi ANSI CSI/OSC, removes unsafe C0 and U+FFF9–U+FFFB characters and carriage returns, and replaces invalid UTF-8; interactive streamed output uses the same filter while provider context retains raw model-tool output. Direct RPC `bash` and `abort_bash` use the shared runner, stream correlated lifecycle updates, return structured output/exit/cancel/truncation data, and retain full logs. Extensions admitted through the existing personal, project-trust, or explicit-path rules can register `user_bash` handlers to claim or decline direct RPC commands, stream correlated updates, and return structured results; handler errors emit `extension_error` and fall through to later handlers or the shell. Direct Bash executions persist as `bash_execution` entries; `excludeFromContext` is retained across save/restore and controls whether a synthetic user message is projected into provider context. Deferred executions are added to agent history after an active turn, counted for history consistency, and included in compaction and HTML export. Tests cover direct chunk forwarding, normalization across split escape sequences, invalid UTF-8 replacement, sanitized spill files, raw model-tool context, default and excluded direct context, restored follow-up prompts, malformed inactive entries, compaction retention, model Bash session environment, stale-environment removal, Unix SIGKILL/SIGTERM exit-code and partial-output results, truncated timeout and abort status with readable full-log paths, handler replacement/decline/error/cancellation, and an actual CLI RPC process with explicit extension loading. `abort_bash` cancels both direct RPC commands and model-invoked Bash tools. Still open: broader exact pinned result/error details, Pi extension event contract, Pi RPC wire event shape, PTY presentation, wider signal/platform behavior, and broad settings reload parity.
   - `edit`: exact/batched original-snapshot matching, BOM/CRLF, strict UTF-8, and Pi's ECMAScript trailing-whitespace set are covered; missing-target errors now match `ENOENT`, and success text matches. `diff`, unified `patch`, and `firstChangedLine` now travel in the tool lifecycle event and persisted history while the model receives only success text. Node differential cases cover replacements, insertions, deletions, empty-file boundaries, duplicate-line ordering, and separated hunks. Edit-focused tests pass 20/20; Release suite 375/375, zero skipped. Linux CI run 36098970353 passed. Still open: permission/error detail differentials and broader filesystem/cancellation cases; the TUI renderer is part of the later TUI phase.
   - `write`: recursive parent creation, successful result text, same-file serialization, symlink following, cancellation and mode behavior have local evidence. Existing file targets reached through a directory symlink now share the mutation queue with their real paths; a deterministic barrier test caught and verifies the fix. Current Pi calls `fsWriteFile` directly with the platform default mode; PiSharp uses atomic replace, creates new Linux files as `0600`, and preserves existing modes. Linux EISDIR, EACCES open, EEXIST and ENOTDIR errors use Node-style messages; the EACCES mkdir branch is implemented but lacks a separate reliable fixture, and other I/O errors retain managed messages. Read-only target permissions, cancellation, atomic replacement and hard-link behavior can still differ. CodingToolsTests pass 19/19; Release suite 375/375, zero skipped. Format verification and the warnings-as-errors Release build pass with zero warnings/errors.
   - `grep`, `find`, `ls`: the managed fallback reads `.gitignore`/`.ignore` outside Git, plus `.rgignore` for grep and `.fdignore` for find. Inside Git it filters all listed candidates using these rules, `.git/info/exclude`, and `core.excludesFile`, preserving higher-priority negations even for Git-ignored files. Git inventory uses NUL-delimited `git ls-files -z`, preserving filenames with embedded newlines. Nested Git repositories are inventoried separately, so `.gitignore` rules stop at each repository boundary; ancestor `.ignore` and tool-specific ignore files continue to apply. Find also reads fd's global ignore file from XDG config or the user config directory, below project rules. Ignore matching supports brace alternatives and character classes alongside wildcard, anchoring, directory and negation patterns, and trims unescaped trailing spaces while retaining escaped trailing spaces. Query glob classes handle a leading literal `]`, and brace alternatives ignore commas inside character classes. The `find` pattern and `grep` glob filters share this parser. Fixtures cover staged/untracked paths, newline filenames, nested repositories and nested ignore boundaries, inherited `.fdignore`/`.rgignore` rules, symlink entries in and outside Git, inaccessible files and directories for grep, and rules inherited from repository ancestors; `find` includes matching empty directories in and outside Git. Find lists symlink entries without walking symlinked directories; grep skips symlinks. Grep matches Pi's 50 KiB head truncation, line marker and separate byte/match notices, and reports unreadable file/directory errors with ripgrep's Linux OS error text in both repository and non-repository searches. SearchToolsTests pass 23/23; full-suite counts are recorded in the Bash evidence below. Still open: remaining ignore-glob syntax, Pi's hidden traversal of `.git` metadata, further fd/rg matching and traversal details, find permission and broken-symlink differentials, and structured renderer details.
   - Acceptance gate: all seven tools' major externally visible behavior is covered and remaining differences are evidenced; then begin the real TUI.
2. **TUI/editor/keybindings:** replace the editor-only UX with a separated terminal, input/keymap, transcript, renderer, status, overlay/picker and application architecture. Cover streaming, reasoning, Markdown/code/tools, resize, scroll/search, Unicode, clipboard, external editor, images, and terminal restoration.
3. **Sessions, RPC/JSON/SDK, settings/resources/themes/extensions and multimodal:** preserve Pi capabilities across PiSharp's internal architecture; implement Pi JSONL and wire-compatible protocol where upstream provides them. Keep capability ownership and trust boundaries explicit.
4. **Provider/auth breadth:** after tools and TUI, add native provider/auth semantics from current Pi; maintain OpenAI Responses and llama.cpp-compatible Chat Completions throughout.
5. **Final audit:** refresh upstream SHA; audit every in-scope CLI, command, agent, tool, TUI, keybinding, session, protocol, settings, provider, image, resource, theme, extension, recovery and error contract. Reconcile the matrix and ledger; run format, warnings-as-errors build, all tests, differentials, representative end-to-end workflows and Linux CI.

## Working decisions

- Agent execution and tools share one Microsoft Agent Framework runtime across interactive, print, JSON, RPC and SDK surfaces. Do not move permanent product orchestration into `Program.cs`.
- Preserve canonical raw history and durable tool outcomes. Never replay uncertain side effects or claim verification from code presence alone.
- Core loop, recovery and compaction already have substantial deterministic evidence. Revisit remaining core edge cases when a new capability exposes a defect, a pinned differential requires it, or final audit shows a material gap; do not spend the next stretch on narrower compaction cases.
- After each meaningful slice: add deterministic tests, update parity evidence and this file, run focused validation, run broader validation as appropriate, commit and push, then continue to the next unresolved capability.

## Immediate next action

Continue with `grep`, `find`, and `ls`; read, Bash, edit, and write have major success behavior and remaining gaps recorded. Start the real TUI once each tool's major observable behavior and remaining differences are evidenced. Keep the known `read` image-processing differences in the final audit without over-polishing this tool.
