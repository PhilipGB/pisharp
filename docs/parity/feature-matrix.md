# PiSharp parity inventory (work in progress)

Pinned reference: earendil-works/pi@b3487650f6378f1b0d1643dd254445ceb4a98035 (fetched 2026-09-25; prior parity baseline 49681e1b71c9c32cdfbf45c21f8e3cb3a8c8629d; earlier refreshed snapshot 5fd446ca1843682e8da3fec4ceb71c42f56fbace). Current Pi TUI is 0.87.1, uses Marked 18.0.11 tokens and exports ANSI-aware grapheme wrapping that hard-breaks overlong words. Current upstream `main` was separately refreshed to `d6af72e1857cfb10b41d8ff8e69f0d72b4cf6d31` for terminal-image and theme inspection; `packages/coding-agent/src/modes/interactive/theme/{theme.ts,theme-json.ts}` and its tests, `packages/tui/src/terminal-colors.ts`, and `packages/tui/src/terminal-image.ts` and its tests include Kitty aspect/cell sizing correction #8938. This scoped review does not replace the durable parity pin; broader changes after `b348765` remain unaudited. Other known upstream deltas include TypeScript 7/Node type stripping, cursor restoration after overlay close, RPC listener snapshot dispatch, null-range full-file read rendering, and per-ref temporary extension cache paths. The selected-model resize profile reaches PiSharp read. The current theme token, selector, settings and terminal-color paths were inspected at the refreshed SHA; CLI theme flags/settings-defined theme paths, partial-viewport image crop/pixel-cell probing, and the broader upstream delta remain open. Paths below are relative to the durable pin; docs/ abbreviates packages/coding-agent/docs/. Status Implemented means a local implementation exists; Verified requires upstream differential evidence. No feature is Verified yet. See the detailed interface inventory for commands, flags, settings, shortcuts and RPC operations. This is not yet a complete source-level audit.

| Category / feature | Upstream documentation / source | Observable behaviour / interface | Error, cancellation, recovery | PiSharp plan / tests | Status / differences |
|---|---|---|---|---|---|
| Agent / multi-turn and streaming | `docs/how-pi-works.md`; `packages/agent/src/agent-loop.ts`, `agent.ts` | Stream text, tool calls and results, continue after tools; session preserves turns | Interrupt, retry, partial response and queued messages per loop | `PiAgent` on MAF `ChatClientAgent`, `AgentSession`; deterministic fake provider tests needed | Implemented and behaviorally verified locally: `RunEventsAsync` orders streamed provider/tool boundaries, preserves MAF turns, retries pre-content transient transport/HTTP 408/409/429/5xx failures (status and metadata-only fixtures), plus a separately guarded one-shot context-overflow 400/413 projection (fake-client and SDK Responses/Completions loopback fixtures plus an opt-in actual llama.cpp Qwen3.8-27B-GGUF overflow/retry, read-tool continuation, tool-result overflow and after-text cancellation runs; Anthropic and retry-header semantics remain open), retains partial failure/interruption evidence, and drains queued RPC prompts as later turns before one settlement. Steering/follow-up behavior is covered by the next row; broad upstream equivalence remains unverified. |
| Agent / steering, follow-up, concurrent tools | `docs/usage.md`; `packages/agent/src/agent-loop.ts` | Escape abort; Enter steering; Alt+Enter follow-up; parallel tool calls | Pending messages returned to editor on abort | application queue and lifecycle tests | Implemented and behaviorally verified locally: active terminal keys route steering/follow-up/abort, steering is inserted at the next MAF provider boundary after tools, one-at-a-time steering precedes follow-up, unsent queues can be restored/cleared, and MAF concurrent invocation is explicitly enabled and overlap-tested. Configurable queue modes and broad upstream equivalence remain separate/unverified. |
| Agent / usage, reasoning, context and compaction | `docs/models.md`, `docs/compaction.md`; `packages/coding-agent/src/core/compaction/` | Streaming reasoning, token/cost accounting, manual `/compact` and automatic compaction | Summarization failure leaves original history intact | context builder and model metadata fixtures | Implemented and behaviorally verified locally: MAF reasoning and structured provider usage stream through application events; branch-local token/cost records survive restart; manual and opt-in pre-prompt automatic compaction preserve raw history and roll back on summary/save failure. Costs require explicit prices and context limits require explicit configuration; the conservative serialized-character context estimate now streams UTF-8 into a UTF-16 counter instead of allocating whole JSON messages (Unicode/1.1M-result regression, not tokenizer parity); opt-in continuation requests now apply a transient whole-turn summary or fail closed before overflowing the configured estimate, with canonical history and summary usage checkpointed; a per-run SHA-256 fingerprint of length-framed per-message digests streams serialized prefix messages into capped hashes to avoid repeating a successful summary across tool continuations without retaining raw cache keys (local two-read and 70k-prefix fixtures; steering invalidation, direct same-length user/call/result prefix-mutation tests, >1MB-message and >16MB-aggregate bypass fixtures; 1MB/message and 16MB total fingerprint caps; summarization separately streams JSON into bounded leading/trailing excerpts). A single pre-content 400/413 overflow request may be retried with a transient whole-turn projection for OpenAI/llama-compatible error strings; fake-client tests reject rate-limit or post-content replay (two additional positive and one negative error strings checked against pinned Pi's `isContextOverflow` classifier as a scoped differential, not full parity); actual OpenAI .NET SDK Responses/Completions loopback fixtures preserve partial text without replay after malformed post-content SSE (including a Completions write-tool turn with a durable outcome, no ordinary chat call/result on abort, and a fresh provider continuation that sees the no-replay notice without rerunning the write); real OpenAI SDK Responses/Completions adapters pass a local HTTP 400 summary/retry loopback fixture, and an explicitly configured actual llama.cpp server with Qwen3.8-27B-GGUF passed a 190052-token pre-content 400 → summary → one streamed retry fixture. Default CI does not run live assertions. A single oversized active turn can be summarized only after matched tool call/result pairs (single-read, two-cycle, one parallel two-read batch and one parallel-batch-plus-third-read local fixtures), rejecting orphan calls, unknown results and reused call IDs in the discarded prefix before summarizing (local fake-client fixtures) and leaving canonical tool history intact; summary requests now retain leading/trailing excerpts of long serialized messages and prioritize recent messages within a 64KB character cap (two local fixtures, not lossless summarization); adversarial multi-cycle splitting, summary fidelity, token-accurate detection, further live backend/adversarial tool-loop overflow cases and branch summaries remain unverified. A [scoped pinned cut-point differential](compaction-cut-points.md) confirms Pi's persistent compaction keeps an oversized trailing tool result where PiSharp's distinct transient request projection summarizes the completed cycle; this is explicitly not upstream equivalence. |
| Models / selection, catalogs and credentials | `docs/models.md`, `docs/providers.md`; `packages/coding-agent/src/core/model-*`, `auth-storage.ts` | `/model`, `/thinking`, `/scoped-models`, `/login`, `/logout`, `--provider`, `--models`, `--thinking`, API key/OAuth | unavailable models and auth failures shown; don't log keys | provider adapters and capability-based selectors | In progress; OpenAI now uses a distinct Responses API adapter with canonical-history isolation (loopback SSE, nonstream usage, function-call/tool-continuation and unsupported-API process fixtures); xAI uses an API-key-only Responses profile with pinned Grok 4.3/4.5/4.6/4.7 static metadata (no assumed /models endpoint); Anthropic now uses native Messages via its .NET SDK with three pinned Sonnet/Opus/Haiku model entries and isolated API key (loopback nonstream body/header/usage, SSE text/tool-result continuation and inline PNG fixtures; unknown model overrides remain unpriced; Anthropic SDK 12.50.0 maps high reasoning to adaptive + effort for pinned Sonnet/Opus and extended + budget for pinned Haiku in loopback, not broad thinking parity); OpenRouter, Mistral and configured compatible endpoints default to Chat Completions; built-in provider endpoint/key-env redirects are rejected and case-variant built-in overrides retain canonical protocol/credential identity (local fixtures); configured Responses models may opt in, unsupported API identifiers fail closed. Still limited user-configured catalog plus built-in OpenAI, OpenRouter, Mistral, xAI, and Anthropic profiles (isolated credentials/endpoints), exact-ID startup without `/models`, partial fuzzy/scoped selection (bounded minimatch subset (*, **, ?, bracket ranges, hidden segments) with local/integration tests and ten pinned Node differential cases; extglobs/braces/escapes/ordering unverified), reasoning option wiring, private API-key storage, saved provider ID, and interactive commands have unit/process fixtures; a Linux PTY fixture now exercises `/login` API-key entry, `/model` switching, `/thinking` capability rejection, `/logout`, and unauthenticated prompt rejection. `--offline`/truthy `PI_OFFLINE` bypass dynamic catalog HTTP (local fake-HTTP fixture; inference is still online). Configured `models.json` has a 1MB read cap and rejects ambiguous provider/model IDs, invalid numeric metadata, and unsupported OAuth declarations (local fixtures). An 8k-ID catalog fixture checks bounded first-wins deduplication, and a malformed-optional-numeric fixture verifies valid sibling models still list; this is local resilience, not upstream differential evidence. A credential-free HTTP process fixture verifies one complete configured-provider turn without `/models` or cross-provider key leakage; configured endpoint overrides cannot redirect the built-in OpenAI identity or bind `OPENAI_API_KEY` to a nonofficial endpoint; anonymous configured providers do not implicitly borrow the local `PISHARP_API_KEY` (unit isolation test). Stored OAuth tokens are refused at the API-key-only provider boundary (unit fixture), rather than sent as API keys or replaced with environment credentials; genuine OAuth refresh and adapters remain missing. An offline `auth check` subprocess fixture verifies credential-presence-only 0/1/2 exit codes without network or secret output; it is not remote readiness. The searchable /model picker opens through Ctrl+L or /model, exposes all/scoped model views through Tab, and shares a reusable terminal overlay/list host; list-state and Linux PTY tests cover filtering, selection, key navigation and draft preservation. Its fuzzy ranking is not differentially matched to current Pi, scoped-model editing/default persistence is absent, and OAuth/hosted-provider gaps remain. No broad native hosted-provider adapters, OAuth authorization/refresh, full provider capability validation, or `/llama` management; no whole feature Verified. |
| Tools / default set, dynamic availability | `docs/cli.md`; `packages/coding-agent/src/core/tools/index.ts` | Default read,bash,edit,write; optional grep,find,ls; `--tools`, `--exclude-tools`, `--no-tools` | invalid name reported | registry with per-run availability and tests | In progress; four defaults, opt-in ls, --tools/--exclude-tools/--no-tools and --no-builtin-tools selectors (extension and explicit builtin loadout fixtures); managed opt-in grep/find/ls and no dynamic extension registry; narrow local tests only |
| Tools / read | `packages/coding-agent/src/core/tools/read.ts`, `truncate.ts`, `../utils/{mime,image-process,image-resize-core}.ts` | Text and images; offset/limit; 2000 lines/50KB, continuation notice | out-of-range, missing file, cancellation | `CodingTools.Read`; unit + upstream fixtures | In progress; small and large text paths decode UTF-8 bytes, preserve UTF-8 BOMs and avoid UTF-16 BOM auto-detection (9 focused tests; direct Node Buffer probe). Large text reads stream a bounded window. Image sniffing covers pinned JPEG/JPEG-LS, static PNG/APNG, GIF, WebP and BMP header cases. SkiaSharp processing validates images, converts BMP to PNG, preserves small supported bytes, and applies 2000×2000 / 4.5 MiB base64 defaults. Configured/discovered model `inputLimits.images.resize` metadata reaches the selected model's `read` tool; tests cover custom dimensions, JPEG quality, a payload cap above the default and field-wise profile merging. Local MAF tool loops send pass-through and high-entropy images through Responses and Chat Completions, save/reload canonical `DataContent`, and cover text-only and `images.blockImages` filtering. Processor tests cover BMP, resize hints, corrupt inputs, pre-cancellation and pre-decode dimension rejection. Remaining differences: cubic vs Lanczos3 sampling, missing EXIF orientation, 20 MiB/32 megapixel safety caps, unverified animated-image transforms, broader filesystem/error details, and no pinned image request/result differential. |
| Tools / write | `packages/coding-agent/src/core/tools/write.ts`, `file-mutation-queue.ts` | Create parents, replace file, cwd-relative or absolute | filesystem failures and cancellation serialize same-file mutations | `CodingTools.Write`; atomic-write/mode/symlink tests | In progress; per-file queue and recursive parent creation are covered, and successful result text matches Pi with no separate details. Existing targets reached through directory symlink aliases now share the canonical mutation lock (Linux barrier test). Linux errors for directory targets (`EISDIR`), denied file opens (`EACCES`), file parent (`EEXIST`) and non-directory ancestors (`ENOTDIR`) use Node-style messages. The EACCES mkdir mapping has no separate reliable fixture. PiSharp atomically replaces files, follows symlinks and preserves existing modes, but creates new files as `0600`; Pi uses direct `writeFile` with the platform default mode. Other I/O error text, read-only target permissions, cancellation and hard-link behavior still differ. |
| Tools / edit | `packages/coding-agent/src/core/tools/edit.ts`, `edit-diff.ts` | Batched unique nonoverlapping `edits[]` against original, BOM/CRLF, diff | ambiguous/missing block aborts entire batch; cancellable | `CodingTools.Edit`; unit tests | In progress; batched original-snapshot edits, BOM/CRLF and fuzzy matching use Pi's ECMAScript trailing-whitespace set; missing targets report `ENOENT`, and success text matches. Edit details now carry `diff`, unified `patch`, and `firstChangedLine` in lifecycle events and persisted history; provider requests receive only the success text. Node differential fixtures cover replacements, insertions, deletions, empty-file boundaries, duplicate-line ordering and separated hunks. Permission/error details, broader filesystem/cancellation differentials and the TUI renderer remain open. |
| Tools / bash | `packages/coding-agent/src/core/tools/bash.ts`, `output-accumulator.ts`, `utils/child-process.ts`, `utils/shell.ts` | Shell command, cwd/environment/shell selection, optional finite timeout, live output, tail truncation | kill process tree on timeout/abort; retain full output; report nonzero exit as tool failure | `CodingTools.Bash`; agent lifecycle/JSON event tests | In progress; resolves bash by platform defaults and validated user/trusted-project `shellPath`; inherits environment and adds the app directory to PATH; model-invoked Bash receives current `PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, and `PI_REASONING_LEVEL` through MAF run context, and inherited stale values are removed before shell start. Direct RPC Bash continues to use the shared shell runner without session metadata. Model-tool lifecycle text deltas coalesce at 100ms, matching the pinned tool path; direct RPC forwards each decoded output chunk immediately, matching `AgentSession.executeBash`. The last 2000 lines/50KiB are returned and complete output spills privately; incomplete UTF-8 flushes; a detached process group is killed on Linux/macOS/FreeBSD when `setsid` is available, otherwise process-tree termination applies; output drain waits for EOF or 100ms of post-exit inactivity, rearming on output. Direct/session Bash strips ANSI CSI/OSC, unsafe C0 and U+FFF9–U+FFFB characters, and carriage returns, replacing invalid UTF-8; the interactive stream display applies the same filter while provider context retains raw model-tool output. Direct RPC `bash` and `abort_bash` use the same runner, return structured result data, persist `bash_execution` entries, project results into provider context by default, and omit them when `excludeFromContext` is true; the flag and result survive save/restore. Extensions admitted by personal, project-trust, or explicit-path rules can register `user_bash` handlers for direct RPC requests: the first non-null result claims the command, handlers can emit correlated updates, and failures emit `extension_error` before the next handler or shell fallback. Deferred records join agent history when the active turn settles, and recent Bash context survives compaction. Fixtures cover cwd, shell selection, both streams, nonzero exits, truncation metadata/full log, timed-out and aborted truncation errors with full-log paths, cancellation cleanup, split UTF-8 and ANSI, Unix SIGKILL/SIGTERM exit codes 137/143 with preserved partial output, direct unthrottled chunk publishing, descendant output after shell exit, live tool updates, direct RPC cancellation barriers, extension replacement/decline/error/cancellation paths, excluded entries through `get_entries` and `get_messages`, model session-environment values, stale-environment removal, raw model-tool output, and the real CLI RPC process. Still open: broader exact pinned result/error details and extension wire contract, Pi RPC wire event shape, signal/platform and PTY presentation, and broader settings reload parity. |
| Tools / grep,find,ls | `packages/coding-agent/src/core/tools/{grep,find,ls}.ts` | Search/list paths with upstream schemas/truncation | symlink/ignore and permission errors | filesystem integration tests | In progress; managed grep/find/ls cover glob, context, limits and listing output. Find and grep query globs support brace alternatives and character classes through a shared parser; fixtures cover leading literal `]` and commas inside a class nested in a brace alternative. Git inventory uses NUL-delimited paths, including filenames with embedded newlines, and discovers nested Git repositories separately. Nested `.gitignore` rules take effect at repository boundaries, while ancestor `.ignore` and tool-specific `.fdignore`/`.rgignore` rules carry through; a nested-within-nested fixture checks both boundaries and local rules. Find includes matching empty directories in and outside Git repositories. It lists symlink entries without descending through directory symlinks; grep skips symlinks in both repository and non-repository searches. Outside Git and inside Git, grep applies `.gitignore`/`.ignore`/`.rgignore`, while find applies `.gitignore`/`.ignore`/`.fdignore`; repository searches also apply `.git/info/exclude` and global `core.excludesFile`. Find reads the global fd ignore file below project rules. Fixtures cover nested and ancestor rules, higher-priority negations, staged files, newline filenames, symlinks, inaccessible file and directory errors for grep, tool-specific sources, brace alternatives, and escaped versus unescaped trailing spaces. Grep preserves complete head lines within 50 KiB, reports byte truncation separately from the match limit, uses Pi's long-line marker, maps unreadable file/directory failures to ripgrep's permission error, and skips NUL-containing files in measured small-file cases. Search/list results preserve Pi's `truncation`, `matchLimitReached`, `linesTruncated`, `resultLimitReached`, and `entryLimitReached` metadata in lifecycle events and session history; a MAF integration test verifies that the provider receives only result text, including after reload. `StructuredSearchToolTests` pass 4/4 and `SearchToolsTests` now pass 24/24. Known source differences include SearchInventory's 20,000-path inventory cap, grep's 10 MiB per-file cap and use of .NET regex/line reading where Pi delegates to fd/rg; direct ripgrep probes also show late NUL bytes can preserve earlier streamed matches where PiSharp skips the whole file. Remaining: glob syntax, fd/rg matching and traversal details, find permission/broken-symlink/cancellation cases, invalid-UTF8 behavior and broader filesystem differentials. |
| Multimodal / input and inline images | `docs/usage.md`; `packages/coding-agent/src/utils/{mime,image-process,image-resize-core}.ts`; `packages/tui/src/terminal-image.ts` | `@image`, clipboard, read image, compatible terminal rendering | reject unsupported image/provider safely | `DataContent`, terminal capability checks | In progress: positional CLI @files pass PNG/JPEG/GIF/WebP as MAF DataContent in print/JSON/initial interactive modes, with 20MB limit and refusal for models explicitly advertising text-only input. Read-tool images are decoded, BMP-normalized, resized and sent through local Responses and Chat Completions tool loops; configured/discovered `inputLimits.images.resize` profiles apply to the selected model's `read` tool, including its provider-boundary payload cap. Positional `@file` image processing still uses defaults. Ctrl+V (Alt+V on Windows) reads a supported clipboard image before falling back to text; Ctrl+X and `/copy` prefer active terminal selection, then copy the latest assistant text using Linux Wayland/X11/Termux commands, macOS/Windows helpers and remote/headless OSC 52 fallback. WSL text clipboard interop, live macOS/Windows clipboard verification, EXIF orientation, Lanczos3 sampling, partial-viewport image cropping, queried pixel-cell dimensions, and pinned image request/result differentials remain open. Interactive Kitty/iTerm2 rendering now covers accepted prompts and read-tool images with conservative protocol detection, safe fallback text, Pi #8938 cell sizing and bounded image memory. Exact code commit `ff301f231d80b0ec81f093cf8d09d5b5b45b821c` passed Linux CI run 36172481288 with format, warnings-as-errors build and 491/491 tests. |
| TUI / normal/fullscreen renderer | `docs/tui.md`, `docs/terminal-setup.md`; `packages/tui/src/tui.ts`, `tui-alt-screen.ts`; `packages/coding-agent/src/modes/interactive/` | header, streaming transcript, markdown, tools, footer, scrollback or alternate screen, resize, mouse | restore terminal on exit/crash | VT renderer, pseudo-terminal snapshots | In progress; persistent alternate-screen compositor keeps the transcript, editor and footer visible while idle and during runs; it restores tty state and captured stdout/stderr on exit; byte-level Linux VT input restores tty and discards bounded OSC BEL/ST color replies without inserting them into the editor; attached TTY sessions query OSC 10/11 and parse validated replies to resolve the light/dark pair, bounded multi-row wrapped editor viewport, and a separated InteractiveTranscript renders lifecycle events, structured search limit/truncation details, and control-filtered interactive text. During runs, the same compositor uses a bounded transcript view, redraw on resize polling, restore captured stdout/stderr plus scrollback on completion, and support configurable PageUp/PageDown/Home/End navigation. Ctrl+Shift+F (Ctrl+F fallback) opens rendered-text search, highlights case-insensitive matches, and cycles the first 10,000 results; closing restores the follow-up draft. Transcript buffer, viewport, search and mouse state are separated into focused components; TerminalScreenCompositor owns frame layout and ANSI output, while TerminalScreen owns lifecycle and application state. Pi-shaped dark/light token palettes load embedded, user, and trusted-project theme JSON; /settings selects named themes or light/dark pairing, /reload rediscovers themes, and Markdown/footer/overlay colors use shared theme tokens with truecolor, ANSI-256, terminal defaults, and NO_COLOR handling. Accepted user prompts and read-tool results can carry in-process DataContent images into a bounded Kitty/iTerm2 renderer; opaque row markers keep payloads out of text layout/capture, and explicit row positioning handles protocol cursor movement. Tests cover Pi #8938 sizing vectors, redraw/delete lifecycle, unsupported fallback, prompt/read event privacy and screen integration. Partial-viewport images still use text fallback; pixel-cell sizing uses the 9x18 default. Exact code commit `ff301f231d80b0ec81f093cf8d09d5b5b45b821c` passed Linux CI run 36172481288 with format, warnings-as-errors build and 491/491 tests. Streaming assistant Markdown now parses through Markdig 1.3.2 CommonMark AST with pipe tables, task lists, emphasis extras/strikethrough, math blocks and autolinks; PiSharp-owned block/inline/table renderers produce terminal styling, visible raw-HTML text, and safe image fallbacks. The renderer covers paragraphs, headings, emphasis, links, lists/tasks, quotes, inline/fenced code, rules and tables; incomplete streaming code/math openers remain readable. Math is rendered as source-like terminal text rather than Pi's Unicode LaTeX layout; syntax highlighting, multiline backslash display math and wider Marked dialect compatibility remain open. Shared TerminalTextLayout measurement/wrapping counts terminal cells, skips SGR/OSC 8 width, preserves grapheme clusters, hard-wraps overlong tokens and carries supported ANSI state between rows; the active transcript and table-cell renderer use it. Tables retain source Markdown only when the terminal is too narrow for the border and minimum cell geometry. Built-in tool calls show short allow-listed argument summaries; finish events retain full text and structured limits. Ctrl+O toggles collapsed/expanded active-run tool results with ten-line previews; full result text replays to stderr within the 4 MB restore cap and the active transcript retains at most 1 MB; live Bash updates remain uncollapsed. Trusted .NET extensions can render tool calls/results through bounded terminal-safe text spans with semantic theme tokens, callback failure fallback and JSON-snapshotted arguments; a compiled extension runs through a loopback Responses provider in a Linux PTY test that checks terminal cleanup. The reusable modal overlay/list host serves searchable all/scoped model selection via Ctrl+L or /model, a searchable project-scoped /resume selector, and a searchable no-argument /fork selector that preselects the newest active-branch user message; direct resume and fork selection remain supported. Focused picker/keymap tests and Linux PTY selection flows cover these workflows. SGR mouse routing covers transcript and prompt drag selection/copy, cell/grapheme-safe editor cursor placement, and modal-list wheel/click interaction; `TerminalMouseRouter` owns interaction routing, and `EditorViewport` maps wrapped rows to source offsets so prompt copy preserves the original draft. Mouse modes are disabled before screen suspend/exit. Synthetic tests cover CJK/emoji cell mapping, inverse highlighting, picker clicks and restoration; `TerminalScreenTests` covers prompt click placement, copy dispatch and selection across a wrapped row. Commit `1abedd5ccde6640d0105da04e592aea02072be1e` passed Linux CI run 36155964704: format, warnings-as-errors build and 455/455 tests. Partial custom-renderer updates and stateful/interactive extension widgets, theme search-path settings/CLI flags, double/triple-click word/line selection, drag autoscroll, mouse-aware extension widgets, full settings schema and controls, cross-project session scope and full session tree/sort/rename/path/trash controls, image crop/resize interaction in scrolled viewports, idle-editor resize notifications, crash-signal restoration and terminal-specific width behavior remain open |
| TUI / editor, history, completion and paste | `docs/keybindings.md`; `packages/tui/src/editor-component.ts`, `autocomplete.ts`, `terminal.ts` | cursor/selection, multiline, history, `/` and `@` completion, bracketed paste, external editor | escape/ctrl+c semantics, Unicode widths | editor state machine + PTY tests | In progress; Tab completes slash-command prefixes and local path tokens, including an empty prompt; the searchable picker supports multiple results, dotfiles except `.git`, directories-first ordering, quoting, wrappers, CJK punctuation and grapheme-safe common prefixes. Keyboard selection/copy, kill-ring/yank, multiline history/undo, external editing, clipboard image/text paste and copy, and configurable named actions are implemented. Unit coverage includes Unicode selection, mutation/undo, kill accumulation and rotation, hidden entries, quoted and wrapped paths, and common prefixes; a Linux PTY selects an `@` completion. WSL text clipboard interop, live macOS/Windows clipboard verification, complete Pi autocomplete and broader application bindings remain open. Prompt mouse selection/copy and grapheme-safe click placement use the terminal mouse router. Exact code commit `17d682c2d7206a052b8a65ca24cf9c514166e39e` passed Linux CI run 36166108417 with format, warnings-as-errors build and 480/480 tests; Linux PTY coverage verifies image paste writes a private image path readable by the agent's read tool. |
| TUI / keybindings, display controls | `docs/keybindings.md`; `packages/coding-agent/src/core/keybindings.ts` | configurable `keybindings.json`, Ctrl+L/P/T/O, Shift+Tab, Alt+Up, session shortcuts | conflicting keys resolved by context | key map and interaction tests | In progress; `keybindings.json` overrides supported editor, interrupt, exit, active-run queue, tool-output toggle, clipboard and alternate-screen scroll/search actions; empty arrays disable them; `/reload` refreshes and `/hotkeys` lists them. Editor bindings take precedence over application shortcuts when both use a key. PageUp/PageDown/Home/End navigate; Ctrl+O toggles active-run tool output; Ctrl+Shift+F (Ctrl+F fallback) searches while the active screen takes precedence over editor cursor movement. Modified cursor/page keys and modified ASCII CSI-u sequences decode. At the idle prompt, Shift+Tab cycles thinking and Ctrl+P/Ctrl+Shift+P (Alt+P fallback) cycle available models while preserving the draft; model changes are saved in the active session. Ctrl+L opens the searchable model selector. `app.session.resume` and `app.session.fork` can be configured with no default binding; the fork action opens the searchable active-branch user-message selector. Ctrl+T thinking visibility, Pi session-navigation/sort/rename/trash key behavior, Kitty protocol negotiation and `super` remain open. |
| Sessions / JSONL lifecycle | `docs/sessions.md`, `docs/session-format.md`; `packages/coding-agent/src/core/session-manager.ts` | `--continue`, `--session`, `--no-session`, `/new`, `/resume`, `/name`, auto-save; project grouping | malformed/partial writes surfaced, recover history | PiSharp-specific application-owned `ConversationSession`, `ConversationRun`, private atomic `ConversationStore` | In progress; the canonical C# tree persists the selected branch and reconstructs MAF history. `PiJsonlSessionInterchange` imports Pi v1-v3 and exports v3, retaining raw Pi entries and migrations for v1 links/compaction indexes and v2 hook messages. Startup `--session <file.jsonl>`, confirmed `/import <path>` and `/export-jsonl` have PTY/process tests; import is limited to the Pi header CWD matching the current project. Existing stale concurrent writer rejection and unknown tool outcome recovery remain separate from Pi JSONL interchange; broader crash/concurrent access parity remains open |
| Sessions / tree, clone, fork, import/export | `docs/sessions.md`; `packages/coding-agent/src/core/session-manager.ts`, `session-export.ts` | `/tree`, `/fork`, `/clone`, `/import`, `/export`, active branch context | navigating never deletes other branches | persisted selected-branch tests with tool calls, failures and restart | In progress; `/tree`, `/branch`, `/fork`, `/clone`, `/new`, `/name`, `/session`, `/sessions`, `/resume <id>`, `/import <path>` and `/export-jsonl [path]` use the canonical store. The v3 exporter serializes all branches parent-first and anchors the selected head; exports use create-new files with user-only Unix permissions. Import requires the recorded Pi CWD to match the current project and does not yet reproduce Pi's cross-project import/session-directory flow. Existing searchable session/fork pickers, private HTML `/export`, manual/automatic compaction and their tests remain; trash, Pi viewer/share, full session-manager lifecycle and broad upstream differential remain open |
| CLI / text, print, JSON, RPC, SDK | `docs/cli.md`, `docs/json.md`, `docs/rpc.md`, `docs/rpc-commands.md`, `docs/sdk.md`; `packages/coding-agent/src/modes/` | protocol framing on stdin/stdout; command correlation, distinct accepted/done; embeddable runtime | protocol-safe errors, shutdown | adapters on common `PiAgent`, contract tests | In progress; `--print`, ANSI interactive editor, experimental `--mode json` and subset `--mode rpc` consume shared app lifecycle; redirected stdin now precedes @file and prompt (local HTTP provider-process order fixture, RPC stdin reserved). RPC supports prompt/abort/state/messages/entries/tree/name/text/commands/manual compact/model discovery, approximate session statistics and private HTML export only. Prompt now responds after preflight with `started`/`queued` disposition; failure is correlated, and active-run prompt queueing requires Pi's `streamingBehavior`. JSONL framing uses literal LF even when the writer's platform newline differs. Events and payloads still carry `format: pisharp`, and the broader commands/events/process contract remains materially wire-incompatible |
| Commands / model & settings | `docs/slash-commands.md`; `packages/coding-agent/src/core/slash-commands.ts` | `/settings`, `/model`, `/thinking`, `/scoped-models`, `/login`, `/logout`, `/llama` | unknown/invalid input feedback | command registry and dialog tests | In progress; `/settings` now edits a supported scalar subset through the shared searchable overlay, with user/trusted-project scope, inherited values, atomic JSON updates, configurable `app.settings.open`, and a Linux PTY workflow; image blocking and pre-prompt compaction refresh the active runtime. Manual `/model <id>`, exact-id startup `--model <id>`, model/session persistence and bounded model discovery remain partial; the full settings schema, capability-driven selector, branch model switching and other commands remain open |
| Commands / sessions & context | same | `/new`, `/resume`, `/name`, `/session`, `/tree`, `/fork`, `/clone`, `/compact`, `/import` | abort leaves consistent session | command/session integration tests | In progress; ANSI editor supports `/new`, `/name`, `/session`, `/tree`, `/branch`, `/fork`, `/clone`, `/sessions`, `/resume <id>`, `/compact`, confirmed `/import <path.jsonl>` and `/export-jsonl [path]`; import currently requires matching project CWD. Model change is separate |
| Commands / export, runtime | same | `/copy`, `/export`, `/export-jsonl`, `/share`, `/bug`, `/trust`, `/reload`, `/hotkeys`, `/changelog`, `/quit` | failed uploads/reloads diagnosed | command and UI tests | In progress; `/quit`, `/trust yes|no|forget`, `/reload` rebuild instructions; `/export [path]` writes private HTML and `/export-jsonl [path]` writes Pi v3 JSONL without overwriting (user-only file mode on Unix); `/share`, `/bug` and other commands remain open |
| Resources / instructions and trust | `docs/configuration.md`, `docs/security.md`; `packages/coding-agent/src/core/{resource-loader,project-trust,system-prompt}.ts` | global/project AGENTS.md, CLAUDE.md, trust before privileged project resources | declined trust ignores project resources | discovery/trust fixtures | In progress; first prioritized AGENTS/CLAUDE file per ancestor and user agent dir reaches MAF with size checks; project trust gates `.pi/SYSTEM.md`/`APPEND_SYSTEM.md`, persisted nearest-parent decisions and CLI overrides; contexts remain trust-independent; project settings outside the validated subset and differential trust fixtures missing; CLI system/append-system prompt overrides now resolve text/files with local and HTTP provider-process tests; `--no-context-files` skips AGENTS/CLAUDE (process fixture); project DLL execution now trust-gated with a narrow fixture; `--no-extensions` prevents discovery even after trust (PTY fixture) |
| Resources / skills, templates, themes | `docs/skills.md`, `docs/prompt-templates.md`, `docs/themes.md`; `packages/coding-agent/src/core/{skills,prompt-templates}.ts`, `packages/coding-agent/src/modes/interactive/theme-selector.ts` | discovery, on-demand skill load, `/skill:name`, template expansion, theme reload | invalid metadata reported | loader, settings and TUI tests | In progress; bounded user/project SKILL.md discovery, descriptions in context and terminal `/skill:name`; direct user/project prompt templates with `$1`, `$@`, defaults and quoted arguments; `--no-skills` and `--no-prompt-templates` skip their respective resources (local fixtures); input expansion works in terminal, print, JSON and RPC with RPC discovery. Embedded dark/light themes and user/trusted-project theme JSON discovery feed `/settings` selection and `/reload`; CLI `--theme`/`--use-theme`/`--no-themes` flags and settings-defined search paths remain absent |
| Extensions / .NET API | `docs/extensions.md`; `packages/coding-agent/src/core/extensions/` | tools, commands, events, context transforms, providers, state, keybindings, UI | hook order, cancellation, exceptions | C# plugin interfaces and lifecycle tests | In progress; public native DLL interface registers MAF functions, terminal commands and bounded per-tool call/result text renderers using host-owned semantic theme styles; renderer failure falls back to standard output, and trusted/user discovery and tool invocation have integration tests. Lifecycle hooks, context transforms, providers, state, keybindings, interactive widgets, resources and broader UI remain absent; TypeScript source compatibility not required |
| Security / settings and authentication | `docs/settings.md`, `docs/environment-variables.md`, `docs/security.md`; `packages/coding-agent/src/core/settings-manager.ts` | precedence, project trust, secrets never exported | project trust is not an OS sandbox | config/trust/security tests | In progress; user and trusted-project settings subsets load default model/provider/thinking, tools, session directory, compaction budgets and image blocking, with user-only defaultProjectTrust with CLI/environment precedence; private user auth storage and project trust for project system prompts/context-independent AGENTS files exist. Only the validated subset of trusted-project settings, no broad settings schema, `/settings`, OAuth or complete credential-source equivalence |
| Pi Packages | `docs/packages.md`; `packages/coding-agent/src/core/package-manager.ts` | install/remove/update/distribute package resources | out of scope by explicit request | none | Excluded: Pi Packages |

## Evidence log

- Durable Pi baseline is `earendil-works/pi@b3487650f6378f1b0d1643dd254445ceb4a98035`. Current `main` was refreshed to `d6af72e1857cfb10b41d8ff8e69f0d72b4cf6d31` on 2026-09-25 for terminal-image work. The scoped `terminal-image.ts`/test review confirmed current Kitty cell sizing chooses the less-distorted adjacent cell count while keeping height limits, reserved rows, placement and crop metadata consistent (#8938). Broader changes since the durable baseline remain unaudited; do not infer provider/session/protocol parity from this image review.
- Upstream baseline: after `npm ci --ignore-scripts`, `npm run hydrate:model-data` and `npm run build:offline`, `npx vitest run packages/coding-agent/test/tools.test.ts --reporter=dot` passed 84/84 at the previous `7c696c00` checkout. This establishes a runnable earlier upstream baseline (`7c696c00`), **not** differential parity evidence at the current pin.
- Captured pinned CLI version/help baseline: [`fixtures/cli-baseline.md`](fixtures/cli-baseline.md). Not yet a PiSharp comparison.
- Pinned edit-planner and renderer-detail fixtures with narrowly matched tests: [fixtures/edit-baseline.md](fixtures/edit-baseline.md). Do not infer full tool parity.
- [Pinned read text-selection fixtures](fixtures/read-text-baseline.md) cover six line/byte cases. The current pin's read source decodes bytes as UTF-8; local tests cover BOM behavior on both size paths and UTF-16 BOM bytes. Image/transport parity remains open.
- [Pinned opt-in ls fixtures](fixtures/ls-baseline.md): three ASCII listing/limit outputs; current code also captures structured limit metadata. The [search detail fixture](fixtures/search-tool-details.md) records the MAF event/history and provider-boundary evidence; no full search/list parity is claimed.
- Pinned upstream `packages/coding-agent/src/cli/file-processor.ts` was inspected at `8676a0d`; current CLI accepts positional UTF-8 text and PNG/JPEG/GIF/WebP `@file` arguments across print/JSON/initial interactive input. Text strips BOM and uses XML-safe file tags; images are passed as MAF DataContent after signature/size checks, with explicit text-only model refusal. Local fixtures cover text, image content, filename escaping, missing files and invalid image signatures. Positional @file image processing still uses defaults; clipboard image paste is covered separately; terminal rendering and provider-wide multimodal parity remain open.
- Editor completion/selection checkpoint: `EditorBuffer`, `EditorKeymap`, `EditorCompletion`, and the shared picker implement keyboard selection/copy, kill-ring/yank, empty-prompt path listing and selection of multiple command/path candidates. `EditorCompletionTests`, `EditorBufferTests` and `TerminalPtyTests.FileCompletionPickerAppliesTheSelectedAtPathThroughLinuxPty` cover Unicode ranges, hidden entries except `.git`, quoted/wrapped paths, CJK boundaries and a real picker interaction. Exact code commit `17d682c2d7206a052b8a65ca24cf9c514166e39e` passed local format verification, warnings-as-errors build and all 480 tests; Linux CI run `36166108417` passed the same on exact head. This is behavioral evidence, not full Pi autocomplete parity.
- Provider catalog metadata coverage was extended against current `packages/ai/src/types.ts` and model catalog formats: local fixtures check model name/API, input modalities, max output token counts (including OpenRouter architecture modalities), conversion of OpenRouter `input_cache_write` pricing, and preservation of configured metadata during live catalog merge. This remains metadata-level local behavioral evidence; no provider/API interoperability is thereby verified.
- Model cost tier behavior was compared with pinned `packages/ai/test/models-runtime.test.ts`: configured `inputTokensAbove` tier switches rates request-wide, and local accounting tests assert both sides of the threshold. Cache-write token counts from recognized `UsageDetails.AdditionalCounts` keys are persisted/emitted and priced at base or selected-tier cache-write rates; catalog parsing and arithmetic fixtures cover this. This remains local behavioral evidence, not provider-wide usage-metadata equivalence.
- [Archived Pi session baseline](fixtures/session-v3-baseline.md): two v3 branch projections and small v1/v2 migration fixtures were compared with pinned upstream in an earlier experimental codec. That codec was removed in favor of a single PiSharp-owned session representation. These archived cases are not active tests and do not establish current JSONL compatibility.
- Local `LocalEndpointTests` checks the OpenAI SDK/MAF adapter POSTs to `/v1/chat/completions` with controlled SSE. Live server `192.168.0.97:8000` advertised Qwen3.8-27B-GGUF as loaded; basic prompt, bash, read/write/edit workflow and saved-session continuation succeeded (see continuation handoff). No upstream differential terminal/protocol evidence.
- Local build and unit results: see the current execution checkpoint in `TODO.md`. Only narrow edit-planner/preview, text read and ls fixtures are active differential tests; archived v3/v1/v2 session cases were compared in an earlier implementation. Other tests check local behaviour and **do not** prove parity.
- Agent-loop application evidence: deterministic MAF fake-provider tests exercise a transient failure before output, retry, streamed tool call/result and continuation, a prompt queued during the continuation, two persisted user turns before one settlement, cancellation, and a post-partial-output failure that is deliberately not retried. RPC process-adapter tests queue a second `prompt` while the first streams and observe both turns before one `agent_settled`. These match the flow described by pinned `docs/how-pi-works.md` and the inspected `packages/agent/src/agent-loop.ts`/`agent.ts`; they are local behavioural evidence, not byte-for-byte upstream parity.
- Steering/follow-up evidence: pinned `docs/usage.md`, `packages/agent/src/agent-loop.ts`, interactive queue handling, and RPC commands were inspected. Deterministic tests queue steering during a blocked tool and prove it reaches the immediate continuation request with the tool result and persists in transcript order; prove steering-before-follow-up ordering; abort and atomically return unsent queues; route Enter/Alt+Enter/Escape and preserve the current editor draft; and use a two-call barrier to prove MAF tools overlap while persisted results retain source order. RPC adapter tests verify distinct `steer`, `follow_up`, and `clear_queue` behavior.
- Usage/compaction evidence: pinned `docs/models.md`, `docs/compaction.md`, `packages/coding-agent/src/core/compaction/`, context projection and model cost fixtures were inspected. Deterministic MAF fixtures stream reasoning plus input/output/cache/reasoning counts, verify explicit-price arithmetic and restart persistence, account summary usage, preserve atomic tool groups, rebuild summary-plus-tail context after restart/branch changes, auto-compact before prompt acceptance, and prove summary/checkpoint failures leave the original head/context intact. Model-catalog fixtures parse context/reasoning and Pi/OpenRouter pricing conventions.
- Next differential fixtures: pinned image-read/bash tool results and event sequences, current canonical sessions versus Pi observed behaviour, JSONL import/export, and CLI protocol comparison.
