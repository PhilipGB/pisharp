# Continuation — incomplete PiSharp implementation

## Baseline and evidence

- Current pinned upstream: `earendil-works/pi@d5629e20489ccf770ed90b5a33941cb3b7ef24d0` (fetched 2026-09-24), with current `main` refreshed 2026-09-25 as `5fd446ca1843682e8da3fec4ceb71c42f56fbace`, in `/tmp/pisharp-upstream`; the 002fc838 baseline below is archival. `TODO.md` is the durable current execution state. Pi Packages are the only *planned* exclusion. Do not call the application Pi-compatible yet.
- The upstream pinned tool suite passed 84/84. Narrow upstream edit/read/ls and historical Pi JSONL fixtures remain in `docs/parity/fixtures`; their previous experimental JSONL implementation was removed to avoid maintaining competing durable histories. **The new canonical format is PiSharp-specific, not Pi JSONL.** Historical fixtures are archival evidence only and no longer executed.
- Current canonical sessions: `ConversationSession` + `ConversationStore` + `ConversationRun` in `src/PiSharp.Runtime/Sessions`. Project-scoped private `.session.json`, atomic replacement, explicit selected branch, MAF history reconstructed from selected path. CLI `--continue`, `--session`, `--session-dir`, `--no-session`, `/tree`, `/branch`, `/fork`, `/clone`, `/new`, `/sessions`, `/resume <id|name>`, `/name`, `/session`, `/model` use that one history. Session discovery avoids mutating optimistic stale-writer hashes; a PTY test covers resume and clone. There is no interactive picker; `/sessions [filter]` searches metadata and `/delete-session <id>` prompts for confirmation before permanent deletion of an inactive indexed session. Tests cover stale catalog hashes, symlink rejection, cross-directory deletion, cancellation, and PTY confirmation. There is no trash support. `/fork` now lists text-only user turns on the active path and `/fork <id>` copies the path before one into a new session with its editable prompt prefilled; it lacks a selector and excludes cross-model forks. Unit tests cover path copying and persistence; a Linux PTY test covers selection, draft clearing and original preservation. `/export` streams a private escaped standalone HTML document of every canonical tree entry, never overwrites an existing path; a PTY test covers it. There is no Pi viewer, JSONL export, RPC export or sharing. Manual `/compact` and RPC `compact` append a validated summary checkpoint after a tool-free MAF summarization request; model context uses summary plus latest whole turn while raw branch chat remains available. Repeated compaction carries the prior summary. An opt-in pre-prompt context budget now runs when `PISHARP_CONTEXT_WINDOW_TOKENS` is supplied; it prefers the latest post-compaction provider usage and adds the pending prompt, falling back to a conservative two UTF-16 characters per estimated token plus overhead when usage is absent. It can trigger a saved summary before accepting a new prompt, preserves raw history, and rejects oversized latest turns rather than splitting tool groups; tests cover ordering, restart and failure rollback. It does not budget inside the MAF tool loop, auto-detect context limits, split turns or retry overflow, and branch summaries remain absent. The former linear MAF snapshot and unused Pi JSONL codec were removed. **Old snapshot files are not migrated.** A tool call/result round trip survives restart without replay. Function failures are persisted explicitly because Microsoft.Extensions.AI intentionally does not serialize exceptions. With a configured store, the run checkpoints its prompt, assistant text periodically, each tool intention before invocation, and tool outcome after invocation; on crash the next open emits an explicit recovery warning for any unknown result, never automatically replays it. Cancellation still records an interruption marker. Output between checkpoints and a tool result before its outcome checkpoint can be lost or remain uncertain; side effects are not rolled back. Ephemeral runs do not checkpoint. Each MAF provider request now retries transient failures at most twice only if no response content was observed; a partial text/reasoning/tool-call attempt is recorded as failed and is never automatically replayed. `ConversationRun` has distinct one-at-a-time steering and follow-up queues. Steering queued during a tool reaches the immediate next provider request and is inserted into MAF/canonical history after that tool result; remaining steering drains before follow-up work. RPC exposes `steer`, `follow_up`, and atomic `clear_queue`; a busy generic `prompt` is follow-up work. Interactive input remains active during a run: Enter steers, Alt+Enter follows up, Alt+Up restores pending input, and Escape/Ctrl+C aborts and restores unsent queues plus the draft. MAF concurrent function invocation is explicitly enabled and overlap-tested, while result history remains model source order. Queue modes are fixed to one-at-a-time and queue persistence across process crashes is absent. Bare v1 text-only sessions can migrate; ambiguous v1 tool turns fail closed. Store saves now use advisory locking plus optimistic conflict detection; stale concurrent writers are rejected. Each save serializes the tree once and flushes the temporary file to disk before atomic replacement; directory fsync and resumable in-flight provider responses are still missing.
- Tools: failed read/write/edit/bash/ls now throw `ToolFailureException`, which MAF exposes as a failing function result; bash cancellation propagates. Large UTF-8 text reads scan the entire file for line counts while retaining only the bounded output window; differential tests compare them with the in-memory pinned text planner. Read-image work covers pinned format sniff boundaries, SkiaSharp decode/validation, BMP-to-PNG conversion, 2000×2000 and 4.5 MiB defaults, configured/discovered selected-model resize profiles, corrupt-image omission, and processor cancellation. Real MAF tool-loop fixtures against local OpenAI Responses and Chat Completions endpoints cover pass-through, a high-entropy resized payload, session reload, text-only models and `images.blockImages`. Remaining read-image differences include Skia cubic vs Pi Lanczos3, missing EXIF orientation, the 20 MiB/32 megapixel resource limits, positional-image profile handling, animated-image transformations, and no pinned image request/result differential. Same-directory atomic write/edit replaces target while preserving Linux mode and following existing symlinks. Bash output remains bounded with private full-output spill; child process group is killed even if shell exits first. Deterministic tests cover tool failure reaching the model, serialization after restart, cancellation, branches and file operations. Exact upstream error/update event parity is **not** established.
- A new bounded OpenAI-compatible `/models` catalog is shared by `--list-models`, `/models`, and RPC `get_available_models`, with a deterministic HTTP credential-isolation fixture. On this run the live `http://192.168.0.97:8000/v1/models` still advertised `Qwen3.8-27B-GGUF` loaded; `--local --list-models` displayed it, and a credential-free `--local --no-session --no-tools --print` prompt returned `PISHARP_CATALOG_LIVE_OK`. This is a narrow smoke test, not tool, cancellation, multimodal or broad provider parity. Earlier, a separate-process `--local --print` then `--continue` smoke run in `/tmp/pisharp-canonical-live-emXetY` recalled `VIOLET-KITE`. This is NOT an upstream differential test; no live credentials should be part of CI.
- An experimental native extension host in `src/PiSharp.Runtime/Extensions/ExtensionCatalog.cs` loads user DLLs and trust-gated project DLLs, sharing host MAF contracts through a collectible AssemblyLoadContext. Public `IPiSharpExtension` can register MAF functions and terminal-only slash commands; `/reload` rebuilds the set. A deterministic DLL fixture tests denied/trusted project discovery, model tool call, command execution under PTY and duplicate rejection. This is neither Pi's TypeScript API nor a complete .NET extension API; plugins execute arbitrary host-privileged code. `README.md` documents the minimal interface.
- Startup `--model <exact-id>` and `--name <label>` now select the OpenAI-compatible model and immediately persist a display name; a separate CLI process test verifies startup metadata and fail-closed explicit model conflicts on resume. Initial session load also catches `InvalidDataException` rather than aborting with an unhandled runtime error. A limited configured OpenAI-compatible provider catalog and fuzzy lookup were recovered later (see recovery section); full hosted-provider parity and per-branch switching remain missing.
- `--session <path|project-id-prefix>` now resolves project IDs instead of treating an unknown ID as a new filename. `--fork <path|project-id-prefix>` copies a same-project source's active path to a newly persisted session without changing its source. A separate CLI-process test checks both and rejects conflicting flags. Cross-project forks, CLI picker and session-id are not supported.
- Session statistics are shared by `/session` and idle-only RPC `get_session_stats`: counts cover active branch messages/turns/tool contents and all tree entries/leaves, while context tokens remain an estimate. MAF `UsageContent` is emitted as structured lifecycle data and persisted as branch-local non-context records; provider token totals survive restart. USD cost is computed only from explicit per-million-token environment pricing and remains `null` rather than invented when any price is unknown. Compaction-provider usage is included. Tests exercise reasoning/usage streaming, cost arithmetic, restart, inactive-branch exclusion and RPC output. A deterministic two-write MAF fixture confirms both results and one continuation request; a separate barrier fixture now proves local function calls overlap when the model returns multiple calls. The loaded local `/v1/models` endpoint still advertised `Qwen3.8-27B-GGUF`; a fresh credential-free `--local --no-session --no-tools --print` live request returned exactly `PISHARP_LIVE_CHECK_OK` (no live tool workflow was verified in this run).
- Idle-only RPC `export_html` now reuses the private, escaping, no-overwrite session HTML writer, returning an absolute path (default under the session working directory). A deterministic RPC test covers inactive branches, custom path, permissions, duplicate destination and invalid outputPath. It is still PiSharp HTML, not Pi's viewer or protocol.
- At this checkpoint format verification and warning-as-error build passed with zero warnings; **123/123** deterministic tests passed, including reasoning/usage accounting, context compaction, a Linux pseudo-TTY interaction test and a process-kill checkpoint/recovery test. Opt-in `.97` JSON/RPC calls returned `PISHARP_JSON_OK` and `PISHARP_RPC_OK`. RPC now also exposes idle-only tree/entries/text/name operations, but still uses `format: pisharp` rather than Pi's wire contract. Full parity remains outstanding.

## Autonomous implementation infrastructure (not a product feature)

`tools/autonomy/controller.py` uses Pi 0.87.1 RPC `agent_settled`, a durable provisional 153-item requirement inventory in `docs/parity/execution-ledger.json`, private supervisor state, flock inheritance, bounded attempts and systemd user service restart to dispatch further slices without the interactive session. The original plan was **not appended** after its heading in the request: `plan_received: false` prevents full completion even if this provisional list is verified. Pi Packages are the only excluded entry. Tests use a deterministic fake RPC worker, not paid inference, and demonstrate dispatch, stop, restart, locking and stalled-task handling. Installed Pi CLI authenticated `openai-codex/gpt-5.6-sol`; a real systemd worker dispatched and settled three pushed/validated slices before hitting the genuine usage limit described below. The service is currently stopped, not operational. This is independent of PiSharp's MAF implementation. See `tools/autonomy/README.md` for opt-in lifecycle and exclusive-writer limitations.

## Recovery 2026-09-24

The systemd controller **stopped** after Codex returned `Codex error: The usage limit has been reached`. Its old implementation treated every `agent_settled` as success and exhausted attempts on 149 unrelated ledger entries while doing no work. Inspection of the persisted Pi JSONL identified repeated assistant `stopReason: error` and the provider message. `tools/autonomy/controller.py` now detects final assistant errors, stops immediately on usage/billing/auth constraints (or generic provider failures), and records the stop reason without consuming a task attempt; deterministic RPC fixture tests cover both cases. The ignored `.pisharp-autonomy/state.json` preserves the incident; do not restart or reset attempts to evade the genuine provider limit. Once the original provider becomes available, a reviewed task-selection recovery is needed; do not dispatch a fresh paid run blindly.

The interrupted provider slice was recovered as **partial** OpenAI-compatible catalog/auth/CLI wiring in `src/PiSharp.Cli/{ProviderModelRuntime,AuthStorage,ThinkingLevels,Program,CliArguments}.cs`, provider metadata and saved provider ID; exact configured IDs no longer require `/models` (a process-kill checkpoint fixture exposed this regression). Unit fixtures check custom-key isolation from `OPENAI_API_KEY`, metadata and fuzzy lookup, scope rejection, and private credential restart/symlink/permissions. Broad hosted providers, OAuth authorization and refresh, per-branch provider switching, provider-feature equivalence and full interactive workflows remain unverified. No full provider capability is marked Verified.

## Provider workflow slice (post-recovery)

A Linux PTY fixture now covers `/login` with hidden API-key entry, a configured `/model` switch persisted in the canonical session, unsupported `/thinking` rejection, `/logout`, and rejected unauthenticated prompts before and after login (no provider request or chat-history mutation). Unsupported `/login ... oauth` now fails before asking for a token; browser authorization remains unimplemented. A credential-free loopback HTTP-process fixture drives one complete MAF streaming turn through a configured provider, verifying that exact model startup sends no `/models` request and sends only its stored key, never an unrelated `OPENAI_API_KEY`. This is **not** hosted-provider or OAuth parity; the provider ledger entry remains in progress.

A separate offline `pisharp auth check --provider <id> [--model <configured-exact-id>] [--local]` now reports local credential presence/source (exit 0/1/2) without provider requests, credential values, sessions or project-resource loading. A separate CLI-process fixture checks absent and private stored keys, rejection of credential printing and model mismatches, and isolation from `OPENAI_API_KEY`. This is **not** Pi's hosted-provider readiness/refresh check; `cli-auth` remains in progress.

A security regression test now forbids `models.json` from redirecting the built-in `openai` provider (including its stored credential) to a custom endpoint or binding `OPENAI_API_KEY` to any nonofficial endpoint; official endpoint detection requires HTTPS on port 443 with `/v1`. Separate provider IDs with separate credentials remain the supported custom-endpoint path.

At this checkpoint, format verification and warning-as-error build passed, and 128/128 credential-free PiSharp tests passed. The provider and CLI-auth ledger rows remain in progress; the original plan is still absent and the Codex supervisor remains stopped on its genuine usage limit.

Standalone `pisharp --export <PiSharp-session-file> [output.html]` now reuses private escaped HTML export without loading provider, project resources or tools; an isolated CLI-process test covers derived/explicit paths, no-overwrite, corrupt and symlink input. It is PiSharp canonical-session-only, not an importer or exporter of Pi JSONL. Format verification, warning-as-error build and the full deterministic suite now pass at 129/129.

## Remaining priority

1. Audit pending checkpoint validity across all branches, corrupted and network-filesystem stores, provider switching and migration policy. Add process-kill tests at more boundaries; unknown side effects are not replayed. Shared `ConversationRun.RunEventsAsync` now drives terminal, JSON, RPC and .NET callers with actual provider/tool boundaries and failure vs completion ordering; improve terminal session controls and queue semantics; lifecycle event channel now bounds pending records to 256 and stalls the provider on slow consumers, with disposal cancellation tested.
2. Terminal uses raw Linux VT input, bracketed paste, partial command/path completion, and a bounded multi-row wrapped editor viewport, tested under PTY and with pure layout tests. Build a full transcript/editor with resize notifications, complete Unicode/terminal-specific width handling, shortcuts, selectors, rich rendering, image display and asynchronous input while streaming. The editor now lays out CJK, combining and joined emoji by grapheme/display cells with unit and PTY Unicode tests; layout updates on the next input after a resize. This remains far from a full TUI.
3. Opt-in managed grep/find exist with narrow local tests; finish upstream differential fixtures, glob/ignore/large-file/UTF-8 conformance. This host lacks `rg`/`fd`; Git supplies ignore rules in repositories and fallback skips common dependency trees. Read processing has local payload-size and format coverage; retain its measured differences for final audit, then cover bash live updates/events, broader multimodal behavior and file bounds.
4. Expand experimental JSON/RPC to full PiSharp functional integrations with shared lifecycle events (byte-for-byte Pi wire compatibility is not required), then provider/auth/settings, model catalog, compaction, skills/templates/themes and native extension API. Update `docs/parity/feature-matrix.md` only with evidence.
5. Project trust gates project system-prompt overrides, skills and direct prompt templates with stored/interactive/CLI decisions; context AGENTS/CLAUDE remains loaded regardless of trust, like Pi. `/trust` and `/reload` rebuild the agent. Extend for settings and executable extensions. Current skill/template discovery and expansion are narrow but shared across terminal, print, JSON and RPC (`get_commands`); full metadata, configuration and programmatic modes remain. No whole feature is Verified.

## Reproduce

```sh
git -C /tmp/pisharp-upstream rev-parse HEAD
dotnet restore PiSharp.slnx
dotnet format PiSharp.slnx --verify-no-changes --no-restore
dotnet build PiSharp.slnx --warnaserror
dotnet test PiSharp.slnx --no-build
dotnet run --project src/PiSharp.Cli -- --local --no-session --print 'Reply hello'
```

## Current execution checkpoint (2026-09-25)

The `read` model resize-profile slice is published in `f882b25` with follow-up evidence in `0a0f41c`; Linux CI passed for both. The Bash audit compared the pinned and refreshed Bash source/test paths, found no path changes between those refs, and recorded the major local contract evidence plus residual wire/PTY/platform gaps in `TODO.md` and the execution ledger.

Edit preview details shipped in `beced084c`; Linux CI run 36098970353 passed. It adds line-numbered `diff`, unified `patch`, and `firstChangedLine` to `tool_execution_finished` details and persisted tool history. Provider requests still receive only the edit success text. Node differential cases cover replacement, insert/delete, empty files, duplicate-line ordering and separated hunks; an MAF tool-turn test verifies details survive conversation save/reload.

The write audit found that an existing file reached through a directory symlink did not share a mutation lock with the real path. `FileMutationQueue` now resolves each existing link component; `CodingToolsTests.FileMutationQueueSerializesExistingTargetsThroughDirectorySymlinks` uses a barrier to verify ordering without sleeps. Focused `CodingToolsTests` pass 19/19; format verification and the Release warnings-as-errors build pass with 0 warnings/errors; the full Release suite passes 375/375 with 0 skipped. Commit `af68552a1` passed Linux CI run 36099478186. Next: continue `grep`, `find`, `ls`, then start the real TUI after each tool's major behaviors and residuals are recorded.
