# Continuation — PiSharp capability parity

`TODO.md` is the concise implementation handoff. The feature matrix records upstream scope and the execution ledger holds per-capability evidence. Pi Packages are excluded unless a core capability depends on them.

## Reference state

The durable parity baseline is `earendil-works/pi@b3487650f6378f1b0d1643dd254445ceb4a98035`, fetched 2026-09-25. Current upstream `main` was rechecked against the remote on 2026-09-26 and remains `d6af72e1857cfb10b41d8ff8e69f0d72b4cf6d31`; terminal-image, theme, session import/CWD and RPC retry/event sources were inspected at that SHA. The current `packages/tui/src/terminal-image.ts` and tests include Kitty cell-size distortion correction (#8938); current theme JSON, parser/controller, selector and terminal-color sources/tests were also inspected. Session sources and tests were refreshed at the same SHA: current Pi uses v3 JSONL entry trees, v1/v2 migrations, and a confirmation-gated `/import <path.jsonl>` that copies into the active session directory, validates/resolves the session cwd, then replaces runtime state. Its import parser tests cover quoted paths, cancellation and missing files; no dedicated upstream session-format document was found. The dark/light JSON palettes embedded in PiSharp match current Pi's files. This ref remains scoped evidence; do not silently replace the durable baseline. Refresh and reconcile upstream before materially changed capability families and the final audit.

## Current implementation and validation

PiSharp uses Microsoft Agent Framework and Microsoft.Extensions.AI for the provider/tool loop, with its own canonical C# session model. Commit `2a10535814e102d17ba4aaefc8bf3512a0a87432` adds Pi JSONL v1-v3 import and v3 export, preserving branches, active head, context edits, compactions, and unknown entries. Startup `--session`, confirmed `/import`, and `/export-jsonl` are covered by deterministic tests. Import currently requires the Pi header CWD to match the running project. RPC prompt responses now follow preflight, return `started`/`queued` dispositions, correlate failures, and use literal LF framing; direct `steer`/`follow_up` return queued dispositions. RPC `set_model` and `cycle_model` validate/select catalog models and replace the active runtime. Commit `1bb382e3f1002b2191afb1b30b24b128d2140aa8` adds branch-persisted RPC `set_thinking_level`, cycling, availability, `get_state.thinkingLevel`, startup restore and Pi JSONL export. Exact Linux CI run `36196277855` passed format, warnings-as-errors build and all 535 tests (0 failed, 0 skipped); focused RPC/session tests passed 29/29. Idle and active changes now emit the top-level `thinking_level_changed` event before the response, with same-level changes suppressed. Queue snapshots plus `agent_start`, `agent_end` with canonical run-local Pi messages and `willRetry: false`, payload-free `agent_settled`, and thinking changes now use Pi top-level event forms. Active-run thinking changes update the next provider request and persist after the current history write. `RpcEventProjector` owns event projection, and `RpcModelCommandHandler` owns model/thinking commands. The agent-end process fixture checks response/start/end/settled order, success, queued turns, abort and provider failure. Provider retry attempts and turn/message/tool/session event families remain open. Linux CI run `36205642095` passed exact source head `38b402e53bf4e0e543e2f2493ecf67ad2765f898` with format verification, a warnings-as-errors build, and 541 tests (0 failed, 0 skipped). `Program.cs` remains 1,051 lines; `PiSessionImportService` is an initial extraction, not a full application decomposition. The TUI uses Markdig 1.3.2 AST parsing with separate terminal renderers, shared terminal-cell wrapping, transcript buffer/viewport/search components, `TerminalScreenCompositor`, a reusable modal picker, focused mouse routing, and token-based dark/light palettes. User and trusted-project themes are selectable through `/settings`; `/reload` refreshes discovery, and attached terminals can report colors through OSC 10/11 for automatic appearance selection. Markdown, footer and overlay surfaces use shared theme tokens. The editor includes multiline history/undo, keyboard selection and copy, kill ring/yank, configurable actions, external editing, text clipboard access, and path/command completion with a searchable picker.

Source head `38b402e53bf4e0e543e2f2493ecf67ad2765f898` passed Linux CI run `36205642095`: restore, format verification, warnings-as-errors build with 0 warnings/errors, and all 541 tests (0 failed, 0 skipped). Pi `main` remains `d6af72e1857cfb10b41d8ff8e69f0d72b4cf6d31`. The earlier loopback listener retry race remains fixed by creating a fresh listener per bind attempt. RPC now projects `agent_start/end/settled` and queue snapshots at a dedicated event boundary; `agent_end` carries canonical run-local messages but `willRetry` is fixed false. Model and thinking handlers were extracted from `RpcMode`. Active-run thinking changes now update the next provider request and persist after the current canonical-history write. Retry attempts, other event families, session CWD switching and application decomposition remain open. Interactive/read images use bounded Kitty/iTerm2 output and safe fallback; partial-viewport cropping and pixel cell probing remain open. A compiled trusted extension renders a provider-backed tool call/result through the Linux PTY, with terminal restoration verified. The broad Pi TUI differential and several recorded TUI residuals remain open.

Source head `3c766cd71fa297c1fa08ee42e371c93b3841e53e` passed exact-head Linux CI run `36207133787`: format verification, warnings-as-errors build, and 543 tests (0 failed, 0 skipped). Locally, the active-thinking/RPC focused set passed 25/25 and the full suite passed 543/543. Current Pi retry implementation and tests were inspected at `d6af72e1857cfb10b41d8ff8e69f0d72b4cf6d31`: session retries follow failed low-level agent runs, `agent_end.willRetry` precedes `auto_retry_start`, and retry attempts begin another low-level run. This differs from PiSharp's existing provider-request retries in `ObservedChatClient`. The next implementation action is to project RPC start/end per low-level turn, segment queued-turn messages, and add the separate session retry controller/events; `Program.cs` remains 1,064 lines and `RpcMode.cs` 509 lines.

## Resume

Project RPC events per low-level turn first, then implement retry state/events at the session boundary without conflating provider-request retries. Preserve failed attempts in the session while omitting retryable attempts from the next model context. Use deterministic event-order, cancellation, tool-continuation, and process tests. Then extract the session runtime boundary needed for cross-project CWD switching and continue the remaining RPC/session families. Settings/resources/extensions, multimodal and provider/auth breadth and the listed TUI residuals remain open; keep them tracked through the current-main audit.
